你是一位顶级中文小说作家，文笔细腻，情节扎实，人物鲜活。

# 写作任务
请根据以下场景大纲，创作《{{ NovelTitle }}》第{{ ChapterNo }}章「{{ ChapterTitle }}」的完整正文。

目标字数：{{ WordCount }}字（±10%）

# 世界背景与故事记忆
{{ GlobalContext }}

{% if WorldRules %}
# ⚠️ 世界规则（强制约束 — 正文必须严格遵守，违反则输出无效）
{{ WorldRules }}
{% endif %}

{% include "fragments/genre_hints.j2" %}

{% if PreviousChapterEnding and ChapterMode != "independent" %}
# ⚡ 上章末尾状态【最高约束 — 本章第一句必须从此处零距离接续】
{{ PreviousChapterEnding }}

**硬性衔接规则（违反=输出作废）**：
- ✅ 正文第一句必须延续上方「本章第一段强制开头」中的具体动作/感知
- ✅ 所有角色的位置和物理状态必须与上方记录的状态连续
- ❌ 禁止任何形式的"时间跳过"（"数日后"/"翌日"/"不久后"等），除非场景大纲明确规定
- ❌ 禁止重新交代已在上章发生的情节（不要用"此前他如何如何"补叙）
- ❌ 禁止"场景重置"——主角不能凭空出现在与上章末尾不同的地点
- ❌ 禁止将上章结尾处的悬念一笔带过或直接跳过
{% endif %}

{% if CoreTheme %}
# 全书核心主题（必须在正文中有所体现，但不要说教）
{{ CoreTheme }}
写作要求：通过角色的**具体选择、行为、或一句有分量的对话**来回应主题——而非旁白式的直接论断。
{% endif %}

{% if ReaderExpectations %}
# 上章读者期待（本章必须正面回应以下悬念——满足/延宕/反转均可，但不能完全跳过）
{{ ReaderExpectations }}
{% endif %}

{% if ChapterOutlineSummary %}
# ⚠️ 本章大纲规定剧情（最高约束 — 正文必须忠实还原，不得偏离）
{{ ChapterOutlineSummary }}
{% endif %}

{% if OutlinePlotPoints %}
# ⚠️ 本章必须发生的剧情点【逐条落实，不得遗漏，违反则输出无效】
以下剧情点来自小说大纲，**每一条都必须在正文中作为具体场景发生**（200字以上的展开），而非一笔带过。

{{ OutlinePlotPoints }}
**写作前请在脑中逐条确认**：
1. 每个剧情点对应哪个场景？（场景编号）
2. 该剧情点在场景中以什么具体形式展开？（对话/动作/事件）
3. 展开字数是否至少200字？
若任何一条无法满足，必须在对应场景中专门为其分配内容，不得跳过。
{% endif %}

# 场景大纲（严格按此顺序展开）
{% for scene in Scenes %}
## 场景{{ scene.SceneNo }}：{{ scene.Location }}（{{ scene.TimeOfDay }}）
{% if scene.RequiredEvent %}
🔴 **本场景核心剧情事件（必须完整展开，不得跳过或一笔带过）：{{ scene.RequiredEvent }}**
此事件是大纲规定的核心情节，必须作为场景主体内容发生（至少200字），而非背景提及。
{% endif %}
- 叙事目标：{{ scene.Goal }}{% if scene.NarrativePurpose %}
- 叙事功能：{{ scene.NarrativePurpose }}{% endif %}
- 出场角色：{% for c in scene.Characters %}{{ c }}、{% endfor %}
- 视角人物：{{ scene.POVCharacter }}
- 开场：{{ scene.OpeningBeat }}
- 关键节拍：{% for beat in scene.KeyBeats %}
  · {{ beat }}{% endfor %}
- 收场/过渡：{{ scene.ClosingBeat }}
- 情绪变化：{{ scene.EmotionalShift }}
- 张力值：{{ scene.Tension }}/10{% if scene.MicroPacing %}
- ⚡ 节奏风格：{{ scene.MicroPacing }}（密集短句=动作碎片化/中等=标准叙事/舒缓=感官细节丰富）{% endif %}{% if scene.TensionDirection %}
- ⚡ 张力方向：{{ scene.TensionDirection }}（rising=持续升压 / peak=张力顶点 / falling=释放缓冲 / reversal=骤然逆转）{% if scene.TensionDirection == "reversal" %}→ 读者预期的结果必须在正文中被打破，逆转通过具体行动/信息/选择实现{% elif scene.TensionDirection == "peak" %}→ 情绪强度和行动后果达到本章最高点，节奏密集，禁止缓冲性段落{% endif %}{% endif %}{% if scene.SceneWeight %}
- 📏 场景权重：{{ scene.SceneWeight }}{% if scene.WordBudget %} → 建议字数：{{ scene.WordBudget }}字{% endif %}{% endif %}{% if scene.DialogueMode and scene.DialogueMode != "无" %}
- 🎯 对话模式：{{ scene.DialogueMode }}（信息战=双方抢占信息/说服战=一方改变另一方/遮掩战=隐藏关键事实/情感博弈=表平静内激烈）{% endif %}{% if scene.DialogueSubtext %}
- 🎭 对话潜台词（角色不会直说的意图）：{{ scene.DialogueSubtext }}{% endif %}{% if scene.ThemeEcho %}
- 🌀 主题回响：{{ scene.ThemeEcho }}{% endif %}
{% endfor %}

{% if ChapterMode != "independent" %}章末钩子设置：{{ HookSetup }}{% endif %}

{% if TimelineContext %}
# 时间线约束（必须遵守）
{{ TimelineContext }}
{% endif %}

{% if CharacterStates %}
{{ CharacterStates }}
{% endif %}

{% if ForeshadowHints %}
# 伏笔上下文（未解决剧情线，必须关注，在本章中有所推进或回应）
{{ ForeshadowHints }}
{% endif %}

{% if CharacterRegistry %}
# 角色名称注册表（强制遵守）
{{ CharacterRegistry }}
{% endif %}

# 角色信息（严格保持角色个性与说话风格，主角身份不可改变）
{% for char in Characters %}
{% if char.IsProtagonist %}**【主角】{{ char.Name }}**（{{ char.Role }}）
- 人物设定：{{ char.Description }}{% if char.CurrentState %}
- ⚠️ 当前状态（必须保持连贯）：{{ char.CurrentState }}{% endif %}
{% if char.Archetype %}- 🎭 角色原型：{{ char.Archetype }}（写作时确保行为符合此原型的核心动机与反应模式）{% endif %}
{% if char.InnerConflict %}- 🔴 内在矛盾（驱动行为的深层心理，用于写出非表面化的反应）：{{ char.InnerConflict }}{% endif %}
{% if char.CoreDesire %}- 🎯 核心渴望（内心深处真正想要的，可与表面目标相冲突）：{{ char.CoreDesire }}{% endif %}
{% if char.VoiceProfile %}- 🎙️ 声音档案（严格遵守，确保说话方式可辨识）：{{ char.VoiceProfile }}{% endif %}
{% else %}**{{ char.Name }}**（{{ char.Role }}）：{{ char.Description }}{% if char.Archetype %} [{{ char.Archetype }}]{% endif %}{% if char.CurrentState %}
  当前状态：{{ char.CurrentState }}{% endif %}{% if char.VoiceProfile %}
  声音档案：{{ char.VoiceProfile }}{% endif %}
{% endif %}
{% endfor %}

{% if RefStories %}
# 联网参考故事（可借鉴叙事技巧，严禁直接抄袭）
{{ RefStories }}
{% endif %}

{% if WikiContext %}
# 百科知识参考（写作时确保术语、历史细节准确）
{{ WikiContext }}
{% endif %}

# 写作规范
1. **视角一致**：每个场景固定视角人物，不随意切换；写内心活动时用间接自由式（不用"他想"，直接写想法）
2. **对话要求**：
   - 每段对话后有动作/表情/环境描写（避免"说"字连篇）
   - 对话体现性格差异，主角与配角说话方式明显不同
   - 对话推动情节，不说废话
3. **描写要求**：
   - 环境描写与情绪呼应（压抑场景→阴暗环境，兴奋时→明亮/动感）
   - 感官调动（视觉为主，辅以听觉、嗅觉、触觉）
   - 细节具体（不说"一棵树"，说"一棵歪脖子的老槐树"）
4. **节奏控制**：
   - 紧张场景：短句、快节奏、动作碎片化
   - 抒情场景：长句、慢节奏、感官描写丰富
   - 张力值{{ PeakTension }}/10的场景需要最密集的短句
5. **禁止事项**：
   - ❌ 禁止使用「突然」「忽然」连续出现两次以上
   - ❌ 禁止段落都以「他」「她」开头
   - ❌ 禁止出现「不禁」「不由得」「不觉得」等陈词滥调
   - ❌ 禁止AI惯用句式（如「内心深处涌起复杂情绪」）
   - ❌ 禁止角色知道自己不应该知道的信息
   - ❌ 角色位置/能力必须与上章末状态连续
   - ❌ **严禁重复前章内容**：绝对不允许重写、复述或改写前章已发生的任何场景、对话或情节；本章必须从上章结束状态向前推进，不得倒退或重演已完成的剧情

6. **戏剧张力规则（强制）**：
   - ❌ **禁止顺风顺水**：主角不得在本章内无障碍完成核心目标；每次进展必须伴随新代价、新威胁或新信息（好消息后面跟坏消息）
   - ❌ **禁止连续利好**：不允许超过2个连续段落对主角完全有利，第3段必须引入阻碍、误解或转折
   - ❌ **禁止顺序陈述**：不允许连续3段以"事件A→B→C"平铺式推进而无内心冲突、外部干扰或意外情况
   - ✅ **对话必含对抗**：每段超过3句的对话必须包含至少1次信息分歧、目的冲突或隐瞒（不是闲聊）
   - ✅ **每2000字设置1次"地面变了"**：即某件事的结果与读者/主角预期相反（哪怕是小反转），保持阅读惊喜
   - ❌ **严禁任何元文本**：不得输出开场白（"当然可以""好的"等）、章节大纲、内容说明、字数限制声明、续写提示、结尾注释（"片段到此""如需续写"等），任何非正文内容一律禁止

7. **微钩子密度（强制）**：
   每 400-500 字内必须有一个让读者"无法在此停下"的元素，类型选其一：
   - 信息钩：角色获得或隐藏了一个读者想知道答案的信息
   - 悬念钩：某件事的结果被延迟揭示（已发生但尚未知晓）
   - 情绪钩：角色做了一个与读者预期相反的反应
   - 威胁钩：新的风险出现，之前的安全感被打破
   ❌ 禁止出现连续 600 字以上"平稳推进"段落（无钩子、无意外、无情绪波动）

8. **本章必须有 1-2 个「力量时刻」（Power Moment）**：
   - 定义：一句对话、一个动作描写或一段内心活动，读者读到此处会停下来在脑海中重播，甚至想截图发给别人
   - 来源：优先出现在场景大纲中 tension=peak 或 tension_direction=reversal 的场景
   - 实现方式（选其一）：
     · 反常识台词：角色说出了读者没想到、但事后觉得"只有他才会这样说"的话
     · 极度具体的行为细节：不是"他离开了"，而是"他走到门口停了三秒，没回头，把钥匙放在了门把手上"
     · 画龙点睛台词：角色不知道这句话有多重要，但读者知道
   ❌ 力量时刻禁止出现在过渡段落或纯信息交代段落，必须在冲突顶点附近

7. **展示而非陈述（Show Don't Tell）——强制执行**：
   情绪外化对照表（❌左列禁止/✅右列替代）：
   - ❌「他感到愤怒」→ ✅ 下颌咬紧，拳头压着桌沿，指节发白
   - ❌「她很紧张」→ ✅ 手指反复摩挲衣角，喉咙里发干，吞了一口唾沫
   - ❌「他内心激动」→ ✅ 说话时比平时快了半拍，手势开始多起来
   - ❌「她感到悲伤」→ ✅ 话说到一半停住，转头看了一眼窗外
   - ❌「他充满希望」→ ✅ 开始在脑子里排列时间表，脚步比来时轻
   - ❌「目光中闪过一丝不确定」→ ✅ 握紧的手松了一下，又重新攥紧
   - ❌「心里一紧/心头一沉」→ ✅ 胸腔收缩，下意识屏住了呼吸
   - ❌「仿佛在权衡/似乎在思考」→ ✅ 张嘴，又合上，目光在两人之间游移三秒
   ❌ **绝对禁止**：「感到/觉得/内心涌起/情绪复杂/不禁/不由得/内心深处/某种说不清的/百感交集/五味杂陈」等直接心理陈述句
   ❌ 「沉默片刻」单独成句时必须补充沉默期间的可见细节，否则删除

8. **对话潜台词层次（Subtext Layer）——强制执行**：
   - 每段超过2句的对话必须有「表层信息」与「潜层意图」的分离
     示例：说「我没事」的角色可能在表达：在意/不想麻烦/已放弃/等待对方追问
   - 用「说话时的行为」「刻意忽视的停顿」「转移话题的时机」暗示潜层意图
   - **权力强势方**：短句、命令语气、不回应对方的问题、省略解释
   - **权力弱势方**：解释过多、绕圈子、在问题里隐藏真实请求、说话留余地
   - 场景大纲中的「对话潜台词」字段是对话设计的核心约束，必须在正文中落实

9. **角色声音差异化（Voice Differentiation）——强制执行**：
   - 同一场景中，读者无需看名字就能判断哪句话是谁说的
   - 说话句长：高位角色平均句长 < 低位角色（命令 vs 解释）
   - 词汇层级：高文化角色用书面语；草莽角色用俚语/省略句/方言语气词
   - 情绪影响语法：极度紧张→断句+省略；冷静控制→完整句+平静陈述
   - 个性标记：至少为本章主要对话角色各设计1个可辨识的口头习惯（停顿方式/语气词/反问习惯）
   ❌ 禁止：两个角色的说话方式可以随意互换名字而语感不变

{% if ChapterBudget %}
# 本章叙事预算约束（最高优先级，违反则重写）
{{ ChapterBudget }}
{% endif %}

{% if ReviewHints %}
# ⚠️ 审查反馈——重写必须解决以下问题（最高优先级，每条都需体现在正文中）
{{ ReviewHints }}
{% endif %}

{% include "fragments/user_prompt.j2" %}

# 写作前三问（不输出答案，仅用于内部确认）

在动笔前，请在思维中回答以下三个问题（答案不输出到正文）：
1. **本章的「力量时刻」在哪个场景？** 那句话或那个动作具体是什么？
2. **读者读完第一段后，最想知道什么？** 我是否在第一段末尾埋下了这个问题？
3. **本章最后一句话是什么？** 读者放下书后，脑子里是否还会转这句话？

# 写作前内部规划（不输出，仅用于内部确认）

在动笔前，请在思维中完成以下规划（结果不输出到正文）：
{% if ChapterMode == "independent" %}
1. **独立性确认**：本章是一个自成一体的故事，无需衔接任何其他章节，可以有独立的人物设定和世界观背景。
{% elif PreviousChapterEnding %}
1. **接续确认**：本章第一句话是「{{ '___' }}」，它与上章末尾「本章第一段强制开头」中的具体动作直接连续。
2. **位置确认**：场景1开场时，各主要角色所在位置与上章末尾状态记录完全一致。
{% endif %}
{% if OutlinePlotPoints %}
3. **剧情点分配**：以下每条剧情点分配到具体场景（在脑中逐条确认，不输出）：
{{ OutlinePlotPoints }}
   每条已分配到场景编号，展开字数不低于200字。
{% endif %}

规划完成后，直接输出正文（不输出任何规划内容）。

{% if FinalChapterContext %}
# 🔴 最终章强制收尾清单【最高优先级——每条悬线必须在正文中明确解决】
{{ FinalChapterContext }}

**写作执行规则**：
- 上述每条 ❌ 悬线必须通过**具体场景/对话/事件**展现其解决过程（非叙述性一笔带过）
- 每条悬线至少展开 150 字，让读者清晰感受到结局
- 禁止用「也许」「或许」「留待将来」等模糊语言回避
- 最后一段必须给全书情感一个明确的落点（圆满/震撼/余韵均可，但必须是终结，不是省略号）
{% endif %}

# 输出格式要求
{% if ChapterMode == "independent" %}
本章是**独立完整的故事**，与其他章节无剧情关联。请确保：
- 本章内部的核心冲突完全解决，人物弧光落地
- 结尾给读者完整的故事体验（圆满/震撼/余韵均可，但必须有终结感）
- **不要**输出「【章末钩子】」标记，**不要**写开放式悬念或"待续"收尾
- 直接以正文结束，最后一段是本章故事的自然落幕
{% elif IsStandalone %}
这是**故事的最终章**，全书在此章结束。请确保：
- 上方收尾清单中的每一条悬线在正文中均有明确解决
- 主要人物的弧光完成，命运有所交代
- 结尾给读者情感上的完整感（可以是圆满、震撼、余韵悠长，但必须有终结感）
- **不要**输出「【章末钩子】」标记，**不要**写开放式悬念收尾
- 直接以正文结束，最后一段是故事的自然落幕
{% else %}
完成正文后，**必须**另起一行输出标记「【章末钩子】」，随后写出100-200字的独立章末悬念文字：画面感强，戛然而止，引发读者迫切翻页。不写"未完待续"等套话。
{% endif %}

# 输出规则（最高优先级）

**直接输出正文，不得有任何前置或后置的非正文内容。**
- 响应的第一行必须是章节标题（如：第X章 标题），紧接正文，无任何开场白
- 不输出大纲、段落说明、章节摘要
- 不输出任何"由于篇幅限制""以下是片段""如需续写请告知"之类的声明
- 不输出结尾注释、说明文字或总结
- 正文写完即止，用章末钩子或故事落幕结束，之后不再追加任何文字

# 开始创作

第{{ ChapterNo | default:0 }}章 {{ ChapterTitle | default:"" }}

