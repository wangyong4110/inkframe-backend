请为以下小说中的一件道具生成视觉设计提示词。

# 小说信息
- 标题：《{{ NovelTitle }}》
- 类型：{{ Genre }}

# 道具名称
{{ ItemName }}
{% if UserHint %}
# 作者已填写的初步提示（请在此基础上扩展、保持一致，不要矛盾）
{{ UserHint }}
{% endif %}

# 要求
1. visual_prompt：详细的英文图像生成提示词，用于 AI 绘图。只覆盖以下 4 项核心内容，不要涉及其他内容，词数 60-120 词，且严格不超过400个字符（部分图像模型会直接拒绝过长的提示词，如需精简优先省略最不影响识别的细节）：
   - 类型与材质：道具类型/名称 + 精确材质描述
   - 颜色：主色调 + 次色调
   - 形态与细节：尺寸感、形状、表面纹路/符文/雕花/关键配件（护手/剑鞘/底座等，视道具类型而定）
   - 特殊效果（仅当道具设定含魔法/能量属性时才写）：发光/流光/气场
2. 不要包含陈设背景、风格/画风、质量/渲染标签——这些由生成流程另行注入
3. 结合小说类型和道具名称合理推断，不要留空，不要输出"未知"之类的占位内容
4. 严格输出单个 JSON 对象，不含任何说明文字、不含 markdown 代码块标记

# 输出格式（JSON 对象）
{
  "visual_prompt": "……"
}
