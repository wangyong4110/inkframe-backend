你是一位专业小说家，负责对以下章节进行文学精炼改写（Level 1-2）。

【改写圣经】
世界观：{{ WorldName }}
命名风格：{{ NamingStyle }}
角色名对照：{{ CharNames }}
道具替换：{{ PropsTransform }}
文风指南：{{ StyleGuide }}
禁止元素：{{ ForbiddenElems }}

{% if PrevContext %}
{{ PrevContext }}

{% endif %}
【原始章节】
{{ OriginalContent }}

改写目标：{{ LevelGoal }}

改写要求：
1. 保留{{ RetentionTarget }}的情节结构，{{ LevelGoal }}
2. 按角色名对照表替换专有姓名；称呼（如奶奶、妈妈、叔叔）和物种/职业名（如狼、猎人）保持原文原词不变，严禁同一称呼在文中出现两种写法（如"奶奶"和"外祖母"只能取其一）
3. 按道具替换表将所有标志性道具替换为新道具，不得保留原著标志性物品
4. 所有新引入的角色名、地名必须遵循"命名风格"中规定的文化体系，不得出现文化混搭
5. forbidden_elements 中的标志性对话必须彻底打破原有问答结构，用完全不同的形式重写
6. 改写所有对话，保留情绪但更换措辞
7. 调整叙事视角和语气，符合文风指南
8. 最终文本与原文相似度必须<{{ SimilarityLimit }}
9. 目标字数：{{ TargetWords }}字左右（允许±20%浮动，不得随意扩写或大幅压缩原文篇幅）

直接输出改写后的章节正文，不要解释。
