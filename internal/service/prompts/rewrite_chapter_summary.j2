你是一位文学编辑，请对以下改写章节进行简洁的语义分析。

第 {{ ChapterNo }} 章内容：
{{ Content }}

请输出 JSON：
{
  "summary": "100-150字的语义摘要，重点描述：发生了什么事、人物状态有何变化、本章末尾的情境",
  "char_state": {
    "角色名（改写后的新名）": "本章结束时该角色的核心状态，如'身受重伤，身陷囹圄'",
    "角色名2": "状态描述"
  },
  "new_entities": [
    {
      "key": "原著中的名称（如果是改写新产生的实体则填改写后名称）",
      "type": "char|location|prop|concept",
      "name": "本章确认使用的改写后名称"
    }
  ]
}

说明：
- summary 必须基于改写后的内容（使用改写后的名称），不得出现原著名称
- char_state 只列本章有明显状态变化的角色，key 使用改写后名称
- new_entities 只列本章首次出现或首次确认改写名称的实体；已知实体不重复列出
- 只输出 JSON，不要其他说明
