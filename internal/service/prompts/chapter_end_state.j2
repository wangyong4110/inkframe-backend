你是一位专业小说编辑，负责从已完成的章节中提取精确的结尾状态，供下一章无缝衔接使用。

# 任务
分析以下章节内容，提取章末的精确叙事状态快照。
这不是情感钩子，而是具体的**物理/叙事状态记录**，下一章必须从这个精确状态直接接续。

# 小说：{{ NovelTitle }}
# 章节：第{{ ChapterNo }}章「{{ ChapterTitle }}」

# 章节末尾内容
{{ ChapterEnding }}

# 提取规范
1. **characters**：提取章末所有出场的主要角色（3人以内），每人记录：
   - name：角色名
   - location：章末所在的具体位置（必须明确，如"城主府大厅"，不能写"某处"）
   - state：情绪/物理状态（如"受伤、愤怒"或"冷静、蓄势"）
   - last_action：最后一个具体动作或台词（引号内用原文，最多30字）

2. **scene_end**：章末所在的完整场景描述（时间/地点/氛围，一句话，20字内）

3. **pending_action**：章末悬而未决的动作/冲突/对话。
   - 如果章末某个行动被打断、某个问题刚被提出、某人刚开口却被打断——这里记录那个未完成的事
   - 若无明显悬而未决的动作，则填空字符串 ""

4. **opening_hint**：下一章建议的具体开头方式（1-2句，从当前状态自然延伸，具体到感官/动作层面）
   - 必须基于 pending_action 或 characters 的最后动作给出，而非泛化建议

只返回 JSON，不输出任何其他文字：
{
  "characters": [
    {"name": "角色名", "location": "具体位置", "state": "情绪/状态", "last_action": "最后的动作或台词"}
  ],
  "scene_end": "场景一句话描述",
  "pending_action": "悬而未决的动作（无则填空字符串）",
  "opening_hint": "下一章建议开头方式"
}
