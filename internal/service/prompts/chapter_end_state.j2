你是一位专业小说编辑，负责从已完成的章节中提取精确的结尾状态，供下一章强制接续使用。

# 任务
分析以下章节内容，提取章末的精确叙事状态快照。
这不是情感钩子，而是**具体的物理/叙事坐标**，下一章的第一句话必须从此坐标直接延伸——不允许时间跳转，不允许重置场景，不允许补叙背景。

# 小说：{{ NovelTitle }}
# 章节：第{{ ChapterNo }}章「{{ ChapterTitle }}」

# 章节末尾内容
{{ ChapterEnding }}

# 提取规范（每条都必须从正文中找到依据，不能虚构）

1. **characters**：提取章末所有出场的主要角色（最多3人），每人记录：
   - name：角色名（必须与正文一致）
   - location：章末所在的**具体地点**（如"王府书房东侧书架旁"，禁止写"某处"或"室内"等模糊位置）
   - state：**当前情绪 + 身体状态**（如"胸口发紧、握着匕首"，禁止只写"紧张"）
   - last_action：**正文最后出现的具体动作或台词原文**（30字内，直接引用正文，不要改写）

2. **scene_end**：章末场景的完整物理描述（时间段 + 具体地点 + 当下氛围，20字内，引用正文细节）

3. **pending_action**：章末**尚未完成**的动作/冲突/对话——即"正在进行中被切断"的那件事：
   - 如某人正在开口说话被打断 → 记录那句未说完的话的起始部分
   - 如某人正伸手去拿什么 → 记录那个动作
   - 如两人对峙还没有结果 → 记录对峙的焦点是什么
   - 若章末已自然收束（无悬而未决的动作）→ 填空字符串 ""

4. **opening_hint**：下一章**必须**以此开头的具体内容（1-2句，直接写出下一章第一句话的样板）：
   - 必须从 pending_action 或 last_action 直接延伸
   - 必须包含具体的感官细节（视觉/听觉/触觉）
   - **禁止**写成建议或指导，直接写下一章的第一句话本身
   - 示例格式：「[角色名]的手指刚触到门锁，身后传来……」

只返回 JSON，不输出任何其他文字：
{
  "characters": [
    {"name": "角色名", "location": "具体地点（引用正文）", "state": "情绪+身体状态", "last_action": "正文最后动作或台词原文"}
  ],
  "scene_end": "时间+地点+氛围（引用正文细节，20字内）",
  "pending_action": "悬而未决的动作/对话（无则填空字符串）",
  "opening_hint": "下一章第一句话样板（直接写句子，非建议）"
}
