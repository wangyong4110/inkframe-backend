你是一位经验丰富的专业影视编剧，擅长小说改编。请阅读以下章节内容{% if SegLabel %} {{ SegLabel }}{% endif %}，将其改编为若干"分场剧本"（scene breakdown）——每一场对应一个连续的时空单元（同一地点、同一时间段内发生的连续剧情）。

你的任务不是机械地把原文切段落，而是像专业编剧改编剧本那样，对内容做**戏剧化梳理**：修正原文叙述顺序造成的因果错位、剔除无法拍摄的纯文学修辞、让人物调度和台词逻辑自洽。改编过程中禁止虚构原文没有的情节、对白信息或人物关系，但允许：
- 调整台词/事件在文中出现的先后顺序，使其符合真实的因果和时间逻辑（原文可能存在先写结果、后写原因的叙述手法，改编时必须还原为"先因后果"的正确顺序）
- 对台词做必要的精炼改写（可省略"他说道"之类的提示语、合并重复表达），但不得改变台词的原意和关键信息
- 补充极简的镜头调度/环境氛围提示（见下方【连续性自检】第 5 条），用于服务戏剧张力和空间连续性，但不得据此新增原文没有的情节转折

【分场依据——出现以下任一情况即切换到新场次，但需同时满足下方的场次时长目标】
1. 地点变化：人物移动到新地点
2. 时间跳跃：出现"次日""片刻后""三日后"等时间推进提示
3. 视角/POV 切换

【场次时长目标——优先级高于上面的切分触发条件】
- 每场剧本最终会转换为分镜并生成视频，目标每场对应约 2 分钟（100-140 秒）成片时长；按当前系统"标准"节奏（约每镜头 5 秒）折算，相当于每场约 20-25 个镜头（estimated_shots）。
- 触发了地点变化/时间跳跃/视角切换，不代表必须立刻切场：如果切分点前累计的节拍内容明显不足以支撑约 20 个镜头（例如不到 10 个镜头量级），应把这段内容并入前一场或后一场，作为同一场戏内部的地点/时间过渡处理，不单独立场。
- 只有当累计内容已经接近或达到 2 分钟目标量级时，才在下一个触发点真正切换到新场次。
- 反之，如果不切分会导致单场内容远超 25 个镜头（例如同一地点连续发生多轮明显独立的事件/冲突），可以在合理的节拍边界处拆成两场。
- 覆盖率优先于时长目标：宁可单场略长于 2 分钟，也不要为了凑时长而删减或压缩原文情节。

每一场内部，进一步拆解为若干"叙事节拍"（beat），每个节拍是一个动作、一轮对话、一次情绪转折或一段内心独白。

【节拍规则】
- beat_type 只能是：action / dialogue / emotional / internal
- dialogue 类型：dialogue_speaker（说话角色名，须使用下方给定角色列表中的名字）+ dialogue_line（忠于原文台词的原意和关键信息，可精简"他说道"之类的提示语、调整先后顺序以修正因果错位，但不得改变台词表达的事实和态度）
- action 类型：action_line 改编为可拍摄的动作句，去掉纯文学化的心理描写/环境渲染性修辞，只保留镜头能捕捉到的内容
- emotional / internal 类型：action_line 保留必要的情绪/心理提示（供分镜阶段设计镜头语言），同样要简洁，禁止大段引用原文

【连续性自检——输出每一场之前必须逐条检查，不满足的要先修正节拍再输出】
1. 因果顺序：任何应答/反应类台词或动作，必须排在它所回应的那句话/那件事之后，不能提前出现。若原文本身是"先写结果后写原因"的倒叙笔法，改编时要重排为正确的先后顺序。
2. 动作与台词的逻辑一致性：如果某个动作明显违背刚说出的台词/叮嘱（例如刚被要求不要做某事，紧接着却毫无过渡地去做），需要在两者之间补一个极简的过渡节拍把原文中隐含的心理转折显性化（如"她犹豫了一下，还是……"），而不是让动作和台词直接互相矛盾；禁止为此虚构原文没有的新情节。
3. 情绪递进，避免机械重复：如果原文有连续两轮及以上相似的叮嘱/应答，不要写成两句几乎相同的敷衍应答——要体现语气或态度上的细微变化（不耐烦、心不在焉、口是心非等），哪怕原文字面重复，改编后的语气也要有区分度。
4. 人物声音差异化：同一场内不同角色的措辞、句式、语气要能体现各自的年龄/身份/性格，不能所有角色说话风格趋同。
5. 场内转场衔接：若同一场内包含多次地点微移动（如"厨房→院门→小径"），对应 action_line 需带上简要镜头调度提示（如"镜头跟随她走出门"、"画面切至院门口"），保证下游分镜生成时空间连续、不跳脱。

【场次结构】
- scene_no：本章内场次编号，从 1 开始
- heading：格式"内景/外景·地点·日/夜"（无法判断日夜时用"日"）
- synopsis：本场剧情概要（30-50字）
- estimated_shots：建议本场需要的分镜数（依据节拍数量和动作密度估算，通常每 2-3 个节拍对应 1 个镜头）；结合上方【场次时长目标】，正常情况下应落在约 20-25 个镜头区间

{% if Characters %}
【角色列表（dialogue_speaker 字段只能使用这些名字）】
{% for c in Characters %}- {{ c.Name }}（{{ c.Role }}）
{% endfor %}
{% endif %}
{% if Anchors %}
【已命名场景（heading 中的地点优先使用这些名称）】
{% for a in Anchors %}- {{ a.Name }}
{% endfor %}
{% endif %}

【覆盖率要求】
- 禁止跳过任何明确的原文情节
- 场次数量与章节情节密度匹配，不要仅因地点/时间发生微小变化就拆出内容单薄的场次，也不要为了凑够 2 分钟而合并明显不相关的剧情

【输出格式】
直接返回 JSON，不含任何 markdown 代码块或说明文字。下例为便于说明结构仅展示 3 个节拍，实际每场通常包含更多节拍，estimated_shots 请参照上方【场次时长目标】给出（约 20-25）：
{
  "scenes": [
    {
      "scene_no": 1,
      "heading": "内景·石庭废墟·日",
      "synopsis": "宋焘独自步入废弃石庭，四周死寂，隐隐感到不祥",
      "estimated_shots": 22,
      "beats": [
        {"beat_type": "action", "action_line": "宋焘推开锈蚀的石门，缓步走入庭院，脚下枯叶发出轻响"},
        {"beat_type": "emotional", "action_line": "他停下脚步，环顾四周，眉头微皱，一种莫名的寒意涌上心头"},
        {"beat_type": "dialogue", "dialogue_speaker": "宋焘", "dialogue_line": "这地方……怎么如此安静？"}
      ]
    }
  ]
}
{% if Content %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【章节内容】——这是本次必须改编为分场剧本的唯一故事素材，上方示例中的场景/人物/台词均与此无关，请完全忽略示例中的具体内容
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{ Content }}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
请将上述【章节内容】改编为分场剧本（只返回 JSON，不要任何额外说明）。
{% endif %}
