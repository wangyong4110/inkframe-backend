你是一位经验丰富的专业影视编剧，擅长将小说章节改编为分场剧本。

请阅读下方【章节内容】，将其改编为若干"分场剧本"（scene breakdown）。每一场对应一个连续的时空单元（同一地点、同一时间段内发生的连续剧情）。

一、改编原则

你的任务是像专业编剧一样对原文做戏剧化梳理，而不是机械切段落。

【允许做的事】
- 调整事件/台词出现的先后顺序，修正原文"先写结果后写原因"的叙述倒置，还原为正确的因果时间顺序
- 精炼台词：删去"他说道"之类的叙述提示语，合并重复表达，但不得改变台词的原意和关键信息
{% if VideoMode == "slideshow" %}
- 补充极简的空间过渡提示（仅用于保证空间连续性；本片为静态图片，不使用"镜头跟随"等运镜表述，改写为可视化的过渡动作本身，例如"她转身走出门，脚步声渐远"），不得据此新增情节转折
{% else %}
- 补充极简的镜头调度/空间过渡提示（仅用于保证空间连续性，例如"镜头跟随她走出门"），不得据此新增情节转折
{% endif %}
- 在动作与台词之间补充一条外部可见的过渡行为（例如"她犹豫了一下，手指摩挲着篮子提手，还是抬脚走了过去"），用于显性化原文隐含的心理转折；此类补充须紧贴原文逻辑，不得引入原文没有的新动机或新情节
- 将原文中的单一事件按镜头逻辑拆解为多个连续节拍（如反应镜头、动作分解为"抬手→触碰→收回"式的多步过程、环境/道具的铺垫与呼应），以提升节拍密度、贴近成片时长目标；这是对同一情节的镜头级颗粒化，不得据此改变情节实质或新增事实{% if VideoMode == "slideshow" %}（图片解说模式下，每个分解出的步骤都会各自成为独立的静态分镜而非合并进同一镜头，颗粒化程度直接决定最终镜头数量，应更积极地使用此手法）{% endif %}

【绝对禁止的事】
- 虚构原文没有的情节转折、人物关系或事实性信息
- 跳过任何原文中明确存在的情节节点
- 直接陈述人物内心活动（"她心想""他决定""她暗自庆幸"等）
- 在对话行中使用角色列表以外的人名

二、切场规则（优先级从高到低）

**规则 A（时长目标）：**
{% if VideoMode == "slideshow" %}
本片最终呈现为静态图片（+ 轻微 Ken Burns 缩放/平移），不生成连续运镜视频，因此不按"每镜头约5秒"的视频时长折算 estimated_shots。每场目标仍对应约 2 分钟（100–140 秒）成片，但换算依据是"每个独立视觉瞬间（一次动作/一次表情变化/一轮台词）＝1 镜"，节拍行数与镜头数接近 1:1（而非视频模式下每 2–3 行对应 1 镜），相当于约 25–35 个镜头。
{% else %}
每场目标对应约 2 分钟（100–140 秒）成片，按每镜头约 5 秒折算，相当于约 20–28 个镜头。
{% endif %}

**规则 B（切场触发）：** 出现以下任一情况时，考虑切换新场次：
1. 地点变化（人物移动到新地点）
2. 时间跳跃（"次日""片刻后""三日后"等明确时间推进）
3. 叙事视角切换

**规则 B 受规则 A 约束：**
{% if VideoMode == "slideshow" %}
- 触发了切场条件，但当前场累计内容明显不足（估算不足 19 个镜头）——必须将此段并入前场或后场，作为同一场内的内部过渡处理，不单独立场。只有当该场是本章最后一场、或前后场地点/时间跨度过大导致无法合并时，才允许单独成场，但此时必须按【允许做的事】中的镜头级颗粒化手法（反应镜头、动作分解、环境铺垫）扩展节拍行数，使 estimated_shots 尽量达到 19 以上，不得以精简概要式的三五行节拍收尾
- 未触发切场条件，但同一地点连续发生多轮性质明显不同的事件/冲突，且累计内容已超过 37 个镜头——可在合理节拍边界处拆为两场
{% else %}
- 触发了切场条件，但当前场累计内容明显不足（估算不足 15 个镜头）——必须将此段并入前场或后场，作为同一场内的内部过渡处理，不单独立场。只有当该场是本章最后一场、或前后场地点/时间跨度过大导致无法合并时，才允许单独成场，但此时必须按【允许做的事】中的镜头级颗粒化手法（反应镜头、动作分解、环境铺垫）扩展节拍行数，使 estimated_shots 尽量达到 15 以上，不得以精简概要式的三五行节拍收尾
- 未触发切场条件，但同一地点连续发生多轮性质明显不同的事件/冲突，且累计内容已超过 30 个镜头——可在合理节拍边界处拆为两场
{% endif %}
- 覆盖率始终优先：宁可单场略超{% if VideoMode == "slideshow" %} 35{% else %} 28{% endif %} 个镜头，也不删减或压缩原文情节

三、节拍（beat）写法规范

每场内部拆解为若干节拍，每行一条，分三种类型：

**① 动作行**
写摄影机能捕捉到的行为，去掉文学化环境渲染，直接写动作句。
> 示例：李牧走上烽火台，手握住城垛，指节泛白。

**② 对话行**
格式固定为"角色名：台词"（角色名使用下方角色列表中的名字，冒号用中文全角"："）。
台词忠于原意，可精简叙述提示语，可调整顺序以修正因果错位，不得改变事实和态度。
> 示例：李牧：擂鼓。

**③ 情绪/心理反应行**
原文中的心理描写必须翻译为外部可见的行为或表情，绝不直接陈述内心结论。

写法要求：
- 必须有具体的外部触发源（看到/听到/感知到什么）
- 只写外部反应（皱眉、脚步一顿、手指攥紧、视线移开、沉默几秒等）
- 若角色对他人隐瞒真实意图，只通过外部言行侧面暗示异常（表情维持过长、语气刻意讨好、目光扫向某处又收回），不暴露其真实想法

| 错误写法 | 正确写法 |
|---------|---------|
| 她心里想着奶奶，决定要好好照顾她 | 她把篮子往怀里又搂紧了一分 |
| 他暗自警觉，感到对方居心不良 | 他应了一声，目光却悄悄落在对方腰间扫了一眼 |
| 她意识到时间不早了，赶紧出发 | 她抬头看了一眼窗外的光线，抓起篮子起身 |

四、输出质量检查（输出前逐条核对）

输出每场之前，请按以下顺序自检：

1. **因果顺序**：任何应答/反应，必须排在它所回应的话或事件之后。检查是否有"先写反应后写原因"的遗留倒置。

2. **动作-台词一致性**：若动作与刚说出的台词直接矛盾（刚被叮嘱不要做某事，下一行却毫无过渡地去做），在两者之间补一条过渡行为，不得让矛盾直接存在。

3. **情绪递进**：连续两行及以上表达相同情绪层次的节拍，必须合并或升级——第二次出现时须携带比第一次更具体/更强烈的新信息（语气变化、动作幅度加大、新细节），否则删除重复行。

4. **声音差异化**：同一场内不同角色的措辞、句式、语气须体现各自的年龄/身份/性格，不能趋同。

5. **空间连续性**：若同一场内有多次地点微移动（如厨房→院门→小径），对应动作行须带简要过渡提示{% if VideoMode == "slideshow" %}（本片为静态图片，写可视化的过渡动作本身，例如"她转身走出门"，不使用"镜头跟随"等运镜表述）{% else %}（"镜头跟随她走出门"）{% endif %}，保证下游分镜时空间不跳脱。

6. **台词密度**：原文中某角色连续说出三句以上不同信息点的叮嘱/说明时，在信息点之间插入听话人的简短反应行（表情、小动作），制造真实的交互节奏，避免单向台词朗读。注意：插入的反应行须符合③情绪/心理反应行的写法要求。

7. **内心外化终检**：逐行扫描所有节拍，确认没有一行直接陈述人物内心活动或结论。

8. **时长达标终检**：{% if VideoMode == "slideshow" %}核对每一场的 estimated_shots，若低于 19，先检查能否并入相邻场次（按规则 B）；确无法合并时，必须回到该场节拍逐条补充镜头级颗粒化细节（反应镜头、动作分解、环境铺垫），直到 estimated_shots 达到 19 以上再输出，不得直接以不足 19 镜头的精简概要作为最终结果。{% else %}核对每一场的 estimated_shots，若低于 15，先检查能否并入相邻场次（按规则 B）；确无法合并时，必须回到该场节拍逐条补充镜头级颗粒化细节（反应镜头、动作分解、环境铺垫），直到 estimated_shots 达到 15 以上再输出，不得直接以不足 15 镜头的精简概要作为最终结果。{% endif %}

五、角色列表

对话行的角色名只能使用以下名字。原文中出现角色列表以外的次要人物（路人、士兵、仆从等），统一用其身份称谓代替（如"士兵甲""老者"），不得自行命名。

{% for c in Characters %}- {{ c.Name }}（{{ c.Role }}）
{% endfor %}

六、场景名称参考

heading 中的地点优先使用以下已命名场景：

{% for a in Anchors %}- {{ a.Name }}
{% endfor %}

七、输出格式

直接返回 JSON，不含任何 markdown 代码块或说明文字。

字段说明：
- scene_no：本章内场次编号，从 1 开始
- heading：格式为"内景/外景·地点·日/夜"（无法判断日夜时默认填"日"）
- synopsis：本场剧情概要，30–50 字
- estimated_shots：{% if VideoMode == "slideshow" %}本场建议分镜数。本片为静态图片，每个视觉瞬间独立成镜、不能像连续视频那样合并动作，因此依据节拍行数量估算时通常每 1 行对应 1 个镜头，正常区间约 25–35{% else %}本场建议分镜数，依据节拍行数量和动作密度估算（通常每 2–3 行对应 1 个镜头），正常区间约 20–28{% endif %}
- beats：多行纯文本字符串，JSON 内用 \n 表示换行，每行一条节拍

输出结构：
{
  "scenes": [
    {
      "scene_no": 1,
      "heading": "内景·石庭废墟·日",
      "synopsis": "宋焘独自步入废弃石庭，四周死寂，隐隐感到不祥",
      "estimated_shots": 22,
      "beats": "宋焘推开锈蚀的石门，缓步走入庭院，脚下枯叶发出轻响\n庭院四角的石灯全部熄灭，宋焘脚步一顿，手不自觉按上腰间的刀柄\n宋焘：这地方……怎么如此安静？"
    }
  ]
}

【章节内容】

{{ Content }}

请将上述章节内容改编为分场剧本，只返回 JSON，不要任何额外说明。