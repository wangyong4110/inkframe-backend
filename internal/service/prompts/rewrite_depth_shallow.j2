{% if RetryHint %}
⚠️【第{{ AttemptNo }}次改写 — 改写要求升级，优先于以下所有指令】
{{ RetryHint }}

{% endif %}
你是一位专业文学编辑，负责对以下章节进行词句精炼（Level 1：字词润色）。

【改写圣经】
世界观名称：{{ WorldName }}
命名风格：{{ NamingStyle }}

角色名替换（逐一严格执行）：
{{ CharNames }}

道具替换（逐一严格执行）：
{{ PropsTransform }}

文风指南：{{ StyleGuide }}

【绝对禁止出现的元素】
{{ ForbiddenBlock }}

{% if ContinuityContext %}
{{ ContinuityContext }}

{% endif %}
{% if PrevContext %}
{{ PrevContext }}

{% endif %}
{% if ArcStage %}
【叙事位置】本章处于「{{ ArcStage }}」阶段，保持与该阶段一致的语气与节奏。

{% endif %}
【原始章节】
{{ OriginalContent }}

改写目标：{{ LevelGoal }}

改写规则：
1. 保留 {{ RetentionTarget }} 的情节、对话内容和事件顺序，不得增删任何情节节点
2. 使用同义替换和句式变换改写每一句话——不允许原文句子原样保留
3. 严格按角色名替换表替换专有姓名；称呼（奶奶/妈妈）、物种名（狼）、职业名（猎人）保持原词，同一称呼全文写法必须统一
4. 严格按道具替换表替换所有标志性道具，不得保留原著标志性物品
5. 所有新角色名/地名必须遵循命名风格，严禁文化混搭
6. 禁止元素中的标志性对话必须改变句式和措辞，不能保留原有问答结构
7. 改写所有对话：保留情绪，但换措辞和句式
8. 调整叙事语气以符合文风指南
9. 目标字数：{{ MinWords }}～{{ MaxWords }}字（原文 {{ OrigWords }}字）

直接输出改写后的章节正文，不要解释。
