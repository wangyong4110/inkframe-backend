你是一位专业的小说角色档案编辑。请对以下章节内容完成两项任务：
① 提取本章首次出现的次要角色，生成角色档案
② 从已有角色列表中，识别哪些角色在本章中出现过

# 小说信息
- 标题：《{{ NovelTitle }}》
- 类型：{{ Genre }}

# 已有角色列表（严禁重复创建其中任何人）
{% if ExistingNames %}{% for name in ExistingNames %}- {{ name }}
{% endfor %}{% else %}（暂无）{% endif %}

# 本章内容
{{ Content }}

---
{% if UserPrompt %}
# ⚠️ 用户指令（最高优先级）

以下指令由用户直接下达，**凌驾于本提示词的所有默认规则之上**，必须无条件执行，不得以任何理由覆盖或忽略：

{{ UserPrompt }}

{% endif %}
# 任务① 规则：新次要角色（new_characters）

## 提取标准
- 只提取本章**有台词、有行动或对情节有影响**的次要角色，纯粹的背景板路人不提取
- 最多提取 **8 个**；若确实没有值得提取的新角色，输出空数组

## 去重与合并（最重要，违反则输出无效）
1. **与已有角色去重**：对照"已有角色列表"，逐一语义判断。若章节中某角色以小名、外号、职称、称谓（"母亲""老板""师父"）等形式出现，但与已有角色列表中某人是同一人，**绝对禁止**重复创建，在任务②中标注出场即可
2. **同批次内去重**：本次 new_characters 数组中不得有两个角色指向同一人（即使名字措辞略有差异）
3. **群体角色合并**：若章节中某类配角以群体形式出现（如"几名侍卫""一群仆人""侍女们"），无论出现几次，**只提取为一个**代表性角色（取最常用的称谓作为名字），不要分裂成多条记录
4. **相似称谓归一**："女吸血鬼仆人""吸血鬼女仆""吸血鬼仕女"若指同一类角色，只保留一个，名字取章节中最常用的那个

## 命名规范
- 使用章节中**出现频率最高**的那个称谓作为 name，避免自造别名
- 若章节中该角色始终以职衔/称谓出现（如"管家"），name 直接用该称谓，不要加小说角色惯用词如"某某管家"

## 字段说明
- role：固定 "minor"
- gender：只能是 male / female / neutral
- age：简短年龄感，如 "16" / "约25岁" / "少年" / "中年" / "老年"
{% if PromptLanguage == "en" %}- description (English): background only — origin, key experiences, relation to main plot. One paragraph, ≤100 words{% else %}- description（中文）：仅写背景——来历、经历、与主线关系，连贯段落，100字以内{% endif %}
{% if PromptLanguage == "en" %}- visual_prompt (English): image generation prompt, ~150 words, covering 12 dimensions in order. No art style, quality, or rendering tags. No violent, gory, death-related, or sexually explicit content.{% if GenreVisualHints %} {{ GenreVisualHints }}{% endif %}

  [1] GENDER & BUILD: gender, age range, ethnicity, height, body type, shoulder-to-hip ratio
  [2] FACE: face shape, three-zone proportion, cheekbones, jawline, chin, eye spacing
  [3] EYES: eye shape, size, iris color (precise hue), default gaze quality
  [4] BROWS (natural): shape, density, color; natural lash length — no cosmetics
  [5] NOSE & LIPS: bridge height, tip shape, nostril width; lip fullness, cupid's bow, bare lip color and texture
  [6] SKIN: precise tone, surface finish, birthmarks or natural distinguishing marks only
  [7] HAIR: color and treatment, texture, density, length, styling, bangs
  [8] OUTFIT (layer by layer): each garment — type, cut, neckline, sleeves, hem, fabric, colors, decorative details
  [9] FOOTWEAR: type, heel, material, color
  [10] ACCESSORIES & CARRIED ITEMS: each item — name, material, style, placement; for weapons/implements describe visual form and ornamentation only
  [11] POSTURE: default standing pose, hand position, characteristic tilt
  [12] SIGNATURE ELEMENT: the single most recognizable visual detail, described with specific material/finish/placement{% else %}- visual_prompt（中文）：图像生成提示词，约150字，按序覆盖12个维度，禁止画风/质量/渲染标签。
  ⚠️ 严禁出现暴力、血腥、伤亡、色情相关敏感词。{% if GenreVisualHints %} {{ GenreVisualHints }}{% endif %}

  [1] 性别体型：性别、年龄感、种族、身高、体型、肩宽、腰臀比
  [2] 面部结构：脸型、三庭比例、颧骨、下颌线、下巴形态、眼距
  [3] 眼睛：眼型、大小、精确瞳色、默认目光质感
  [4] 眉毛（自然）：眉形、密度、眉色；自然睫毛形态——禁止任何妆容描述
  [5] 鼻唇：鼻梁高度、鼻头形态、鼻翼；唇厚、唇峰、裸唇色与质感
  [6] 肤色：精确肤色描述词、肤感，仅可写先天性标记（痣、胎记）
  [7] 发型：发色与处理方式、发质、发量、发长、造型方式、刘海
  [8] 服装（逐层）：每件——品类、廓形、领口、袖型、下摆、面料、颜色、装饰细节
  [9] 鞋履：鞋型、跟高、材质、颜色
  [10] 配饰与随身物件：逐件描述外观——名称、材质、风格、佩戴位置；兵器/法器仅写形制与装饰
  [11] 体态：默认站姿、手部位置、特征性倾斜
  [12] 标志性元素：最具辨识度的单一视觉细节，具体描述材质/光泽/位置{% endif %}
- personality_tags：2–4个简短性格标签

---

# 任务② 规则：已有角色出场识别（appearing_characters）
- 从"已有角色列表"中，找出本章**确实出现、被提及或直接参与情节**的角色
- **语义匹配**：同一人物可能以姓名、简称、称谓、外号、职衔等多种形式出现，只要判断是同一人即视为出场
- 输出已有角色列表中的**原始名字**（字符串数组）；若无已有角色出场，输出空数组

---

# 输出（严格 JSON，不含任何说明文字）

```json
{
  "new_characters": [
    {
      "name": "角色名",
      "role": "minor",
      "gender": "female",
      "age": "中年",
      "description": "背景故事……",
      "personality_tags": ["冷静", "忠诚"],
      "visual_prompt": "……"
    }
  ],
  "appearing_characters": ["已有角色名A", "已有角色名B"]
}
```
