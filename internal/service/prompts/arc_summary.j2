你是一位专业的小说编辑。**你的唯一输出是一个 JSON 对象，不得输出任何正文、标题、说明文字或 markdown 代码块。**
请为以下章节段落（第{{ StartChapter }}-{{ EndChapter }}章）生成弧光摘要。

# 任务
这段内容是《{{ NovelTitle }}》第{{ ArcNo }}弧（第{{ StartChapter }}-{{ EndChapter }}章）。
请生成一份供后续章节写作参考的结构化摘要，帮助AI写作时保持长期连贯性。

# 章节信息列表（摘要 + 原文节选）
{% for ch in ChapterSummaries %}
【第{{ ch.ChapterNo }}章「{{ ch.Title }}」】
摘要：{{ ch.Summary }}{% if ch.OpeningExcerpt %}
章首节选：{{ ch.OpeningExcerpt }}{% endif %}{% if ch.EndingExcerpt %}
章末节选：{{ ch.EndingExcerpt }}{% endif %}{% if ch.Hook %}
章末钩子（伏笔/悬念）：{{ ch.Hook }}{% endif %}
{% endfor %}

⛔ 直接输出 JSON 对象，第一个字符必须是 `{`，不要使用 ```json 代码块，不要任何前缀文字：
{
  "arc_summary": "本弧整体情节概述（200-250字，包含起承转合及本弧最重要的叙事转折）",
  "key_events": [
    {"chapter": 章节号, "event": "关键事件描述（40字内，含人物+结果）"}
  ],
  "character_states": {
    "角色名": "本弧结束时角色的具体状态（位置+能力/实力等级+关键关系+心理变化，50字内）"
  },
  "relationship_map": [
    {"pair": "角色A与角色B", "status": "本弧结束时的关系状态（盟友/敌对/暗中监视/未知，含原因）"}
  ],
  "open_foreshadows": [
    "尚未兑现的伏笔（含埋下章节号和预期兑现方向）"
  ],
  "resolved_conflicts": [
    "本弧已解决的冲突/伏笔（供后续章节确认不再提及）"
  ],
  "world_updates": "世界观/势力格局的重要变化（如无则留空）",
  "protagonist_power_level": "主角在本弧结束时的能力/实力状态（供后续章节保持连续，禁止降级）",
  "peak_tension_chapter": 张力最高的章节号,
  "emotional_tone": "本弧整体情感基调"
}
