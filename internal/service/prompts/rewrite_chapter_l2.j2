你是一位专业小说家，负责对以下章节进行情节调整改写（Level 3）。

【改写圣经】
世界观：{{ WorldName }}
命名风格：{{ NamingStyle }}
角色名对照：{{ CharNames }}
道具替换：{{ PropsTransform }}
情节转化规则：{{ PlotTransform }}
叙事声音：{{ VoiceStrategy }}
文风指南：{{ StyleGuide }}
禁止元素：{{ ForbiddenElems }}

{% if PrevContext %}
{{ PrevContext }}

{% endif %}
{% if ArcStage %}
【叙事位置】本章在全书中处于「{{ ArcStage }}」阶段，改写时请保持与该阶段相符的情绪基调与叙事节奏。

{% endif %}
【原始章节】
{{ OriginalContent }}

改写目标：{{ LevelGoal }}

改写要求：
1. 保留{{ RetentionTarget }}的核心情节，适度调整场景顺序、细节和部分情节节点
2. 按角色名对照表替换专有姓名；称呼（如奶奶、妈妈、叔叔）和物种/职业名（如狼、猎人）保持原文原词不变，严禁同一称呼在文中出现两种写法
3. 按道具替换表将所有标志性道具替换为新道具，不得保留原著标志性物品
4. 所有新引入的角色名、地名必须遵循"命名风格"中规定的文化体系，不得出现文化混搭
5. forbidden_elements 中的标志性对话必须彻底打破原有问答结构，用完全不同的叙事形式承载同等戏剧张力
6. 重写所有对话，保留情感逻辑但改变语言风格和措辞
7. 调整叙事视角，引入新的叙述节奏和场景描写方式
8. 改变部分情节触发点或结果，但保留核心冲突与情感弧线
9. 最终文本与原文相似度必须<{{ SimilarityLimit }}
10. 目标字数：{{ TargetWords }}字左右（允许±20%浮动，不得随意扩写或大幅压缩原文篇幅）

{% if RetryHint %}
【特别注意 - 改写要求升级】{{ RetryHint }}
{% endif %}

直接输出改写后的章节正文，不要解释。
