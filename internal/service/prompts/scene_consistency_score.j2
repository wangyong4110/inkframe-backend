你是影视/动画制作领域的视觉一致性评估专家。

场景锚点："{{ AnchorName }}"
参考图：{{ RefImageURL }}
生成图：{{ GeneratedImageURL }}
锚点描述：{{ Description }}

## 任务
对照锚点描述比较两张图片，从五个维度打分（每项 0.0-1.0）。

## 维度定义与评分标准

**arch_score —— 建筑结构**：结构、格局、空间规模、关键物理特征（权重 0.35）
- 1.0：所有结构元素均匹配（柱子数量/位置、天花板类型、门窗位置、整体空间比例）
- 0.9：次要元素有轻微偏差，核心结构正确
- 0.7：一项主要结构元素错误或缺失（如天花板高度错误、缺少柱子）
- 0.5：多处结构不匹配，仅大类相符
- 0.3：仅场景类别（室内/室外）相符，具体细节全部错误

**light_score —— 光线**：光源方向、色温、亮度、阴影质感（权重 0.25）
- 1.0：光源方向、色温、阴影形态均与参考图一致
- 0.9：某一方面有轻微偏差（如色温略偏暖/偏冷）
- 0.7：光线方向正确但色温偏差超过 1000K，或缺少阴影
- 0.5：光线方向错误或色温完全错误
- 0.3：光线完全相反（如逆光 vs 顺光、白天 vs 夜晚）

**atmo_score —— 整体氛围**：氛围、情绪、色彩调色板、情感基调（权重 0.20）
- 1.0：色彩调色板、大气粒子、情感基调均匹配
- 0.9：色彩略有偏差但情感基调一致
- 0.7：色彩调色板部分错误或缺少关键氛围粒子（雾/尘/烟）
- 0.5：情感基调错误（如本应压抑却显宁静）或色彩明显偏移
- 0.3：氛围或色彩调色板完全不同

**prop_score —— 关键道具**：使场景可辨识的道具、装饰、标志性元素（权重 0.15）
- 1.0：所有标志性元素齐全且位置正确
- 0.9：多数元素齐全，位置略有偏差
- 0.7：缺失或替换了一项关键标志性元素
- 0.5：多个标志性元素缺失
- 0.3：无任何可辨识的锚点标志性元素

**time_score —— 时间/季节一致性**（权重 0.05）
- 1.0：时间和季节与参考图一致
- 0.7：时段大类一致（白天/夜晚）但具体不同（黎明 vs 正午）
- 0.3：时段大类错误（白天 vs 夜晚）

## 加权总分公式
overall_score = arch_score × 0.35 + light_score × 0.25 + atmo_score × 0.20 + prop_score × 0.15 + time_score × 0.05

## 判定阈值
- overall_score ≥ 0.85：PASS（一致性可接受）
- 0.70 ≤ overall_score < 0.85：NEEDS_RETRY（需调整提示词后重新生成）
- overall_score < 0.70：NEEDS_HUMAN（需人工复核）

## 输出要求
- 若 overall_score < 0.85：在 `issues` 中列出具体问题
- 若 overall_score < 0.85：在 `suggested_fix` 中给出逗号分隔的提示词关键词/短语，用于在下次生成时补充加入以修复已识别的问题（如"增加朱红色柱子，提高琥珀色火炬光 2800K，降低雾气浓度，增加香烟"）
- 若 overall_score ≥ 0.85：`issues` = [] 且 `suggested_fix` = ""
- 每个问题需分类标注：category（arch/light/atmo/prop/time）、severity（score≥0.7 为 warning，score<0.7 为 error）、detail（具体差异描述）

只输出合法 JSON，不要 markdown：
{
  "overall_score": 0.0-1.0,
  "arch_score": 0.0-1.0,
  "light_score": 0.0-1.0,
  "atmo_score": 0.0-1.0,
  "prop_score": 0.0-1.0,
  "time_score": 0.0-1.0,
  "issues": [
    {"category": "arch|light|atmo|prop|time", "severity": "warning|error", "detail": "specific discrepancy description", "score": 0.0-1.0}
  ],
  "suggested_fix": "comma-separated prompt keywords to add for next generation attempt"
}
