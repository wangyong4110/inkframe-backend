请为以下小说中的一个角色生成简介。

# 小说信息
- 标题：《{{ NovelTitle }}》
- 类型：{{ Genre }}

# 角色名称
{{ CharacterName }}
# 角色定位
{{ CharacterRole }}
{% if UserHint %}
# 作者已填写的初步描述（请在此基础上扩展、保持一致，不要矛盾）
{{ UserHint }}
{% endif %}

# 要求
{% if PromptLanguage == "en" %}1. description (English): a single coherent paragraph covering appearance, personality, and background — physical appearance and distinguishing features, personality traits and mannerisms, origin/backstory and how they came to their current role. 150-250 words{% else %}1. description（中文）：写成一段连贯文字，需覆盖外貌、性格、背景三个方面——外貌与显著特征、性格特点与言行举止、出身经历及如何成为现在的身份定位。150-250字{% endif %}
2. 结合小说类型和角色定位合理推断，风格和用词要符合该类型小说的气质，不要留空，不要输出"未知"之类的占位内容
3. 严格输出单个 JSON 对象，不含任何说明文字、不含 markdown 代码块标记

# 输出格式（JSON 对象）
{
  "description": "……"
}
