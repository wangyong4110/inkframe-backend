以下角色名单是从小说《{{ NovelTitle }}》的多个章节独立提取的，可能存在重复或别名（同一角色在不同章节被以不同名称记录）。

# 待整合名单
{{ Names }}

# 要求
1. 识别并合并所有重复/别名角色，例如：
   - "大灰狼"与"狼"是同一角色 → 保留"大灰狼"
   - "孙悟空"与"猴子"是同一角色 → 保留"孙悟空"
   - "小红帽"与"小红帽的外婆"是不同角色 → 分别保留
2. 保留最完整、最具辨识度的名称
3. 合并时保留 role（protagonist/antagonist/supporting）和 brief，取最准确的那条
4. 最多保留 8 个主要角色
5. 严格输出 JSON 数组，不含任何说明文字

# 输出格式
[{"name": "角色名", "role": "protagonist", "brief": "一句话描述"}]
