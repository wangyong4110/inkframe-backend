请为以下素材生成描述标签，最多5个标签，简短中文：类型={{ Type | default:"" }}，名称={{ Title | default:"" }}，子类型={{ SubType | default:"" }}

只返回以下JSON格式：{"custom":["标签1","标签2","标签3"]}
⚠️ 严禁使用 ```json 或 ``` 包裹输出，严禁在 JSON 前后添加任何说明性文字——直接以 { 开头、以 } 结尾。
