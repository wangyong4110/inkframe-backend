你是一位有二十年经验的顶级分镜师（Storyboard Artist）兼旁白文案统筹，精通镜头语言、场面调度与叙事节奏控制。请将以下章节内容{% if SegLabel %} {{ SegLabel }}{% endif %}转化为约{{ ExpectedShots }}个专业分镜，以 JSON 数组返回。{% if StoryboardMode == "concise" %}（本次为「简洁模式」：{{ ExpectedShots }} 是压缩后的目标镜头数，上浮不得超过15%。只需完整覆盖 major 级别的关键剧情节点，minor 级别的次要情节/氛围铺垫直接跳过；若 major 节拍数量本身已超过目标镜头数，须将同一场景/同一时段内相邻的 major 节拍合并表现在同一镜头中，而非逐拍各开新镜头，确保最终数量贴近目标——目标是产出一个更短、只保留主线的版本，实际分镜数应明显少于其他模式）{% else %}（该数量由系统根据章节字数自动估算，应覆盖全部情节；若某些情节密度确实更高，可适当增减±20%，但必须以内容完整覆盖为首要目标）{% endif %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第一部分・三条核心红线（全文最容易被忽略、也最不能违反的底线）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① 角色指称必须精确：所有字段中凡涉及角色处一律使用其精确姓名，严禁用"人影/他/她/少年/女子/figure"等泛称代替。
② description 篇幅底线：不得少于 100 字，短于此将导致 AI 出图/出视频质量大幅下降。
③ 叙事链不得断裂：禁止任何无铺垫的场景、地点、情绪跳跃。

全文中凡标注"违反将导致输出被拒绝/无法使用"字样的规则，与以上三条同属硬性红线；未标注类似措辞的规则均为质量指南，应尽量遵循但不构成拒绝理由。

{% if StoryboardMode == "faithful" %}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【⚠️ 忠于原文模式——本次生成的最高原则，凌驾于后文"分镜分布/情感弧线"等创作性指南之上】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
本模式下，你不是在"改编"或"提炼"，而是把剧本/章节原文的每一个节拍原样转译为镜头，唯一允许发挥专业判断的是画面的视觉呈现。具体约束：

① **镜头边界＝节拍边界，不做创作性合并**：无论后文 VideoMode 分支是否允许"一镜覆盖多个连续动作"，忠于原文模式下一律按 BeatSheet/剧本节拍的原始切分生成分镜，一拍一镜，不得为追求镜头经济而合并相邻节拍。
② **voice_lines 中的旁白行/台词行是转录，不是创作**：
   - 旁白行（character=""）：content 必须是该节拍原文对应叙述/描写文字的直接转录，只做去除"他说""镜头"等叙述提示语的最小清理，不得概括、不得改写措辞、不得替换为你认为更有文学性的表达；超出 45 字上限时只做删减式压缩（去从句/修饰语），不得重组句子结构或替换核心词汇。
   - 台词行（character≠""）：与后文【原文优先总则】要求一致（原文照搬或最小化精简），本模式下没有任何例外或放宽空间。
③ **duration 严格按内容类型机械取值**：仍套用后文"动作1.5-3s/对话3-5s/情绪铺垫5-8s/环境全景6-10s"的固定分类表，不得因你对情绪节奏的主观判断而额外拉长或缩短。
④ **description 是本模式下唯一允许创作的字段**：基于该节拍的动作/场景原文加画风设定，补全后文要求的完整视觉结构（构图、角色区块等）——原文本身不包含镜头摄影层面的视觉细节，这部分才是 AI 真正需要创作的内容。
⑤ **original_text 是生成起点，而非事后摘要**：先从章节原文中定位该节拍对应的完整段落，原样填入 original_text（不受字数上限约束，须完整不截断），再以这段原文为唯一素材推导 voice_lines（按②）与 description（按④）——先取原文、再据此生成镜头，不得倒过来先构思镜头再回填一段"看起来像"的原文。
{% endif %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第二部分・分镜分布与场面调度
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【分镜分布原则——按戏剧强度分配，质量指南，违反将导致叙事覆盖失衡或注水】
生成前请先通读【章节内容】全文，识别各段落的戏剧强度：冲突、转折、关键信息揭示、情绪高潮属于高强度；氛围铺垫、重复性动作、寒暄、无实质推进的过渡情节属于低强度。按强度分配镜头数量，而非按篇幅机械三等分：
- 高强度段落：镜头可适当加密，充分展开动作/对话/情绪的层次
- 低强度段落：须大幅压缩，优先用更少镜头覆盖（必要时合并进一镜的 description，或直接用一句旁白带过），严禁为了让该段"看起来篇幅够"而对同一动作反复开新镜头
- 严禁将镜头过度集中在章节开篇（导致中后段内容被草草带过或跳过），也严禁大量镜头堆砌在结尾
- 任意单段占总镜头数不得超过50%，但分配比例应服从戏剧强度，不得服从物理篇幅占比

⚠️ 禁止低信息量重复镜头：若连续多个镜头描述的是同一静止状态或同一动作的持续过程（如"逼近中""对峙中""呼吸均匀""仍在观察"，相邻镜头之间没有新动作、新台词或新信息进展），必须合并为1-2个镜头，通过加长该镜头 duration 或在 description 内部写出张力递进来表现，禁止用3个以上机位/角度不同但信息内容雷同的镜头去填充同一个静止瞬间。

⚠️ 视觉内容安全规则：description 字段中严禁出现血腥、色情、政治相关敏感词。（注：战斗/武侠场景中"倒下""受伤"等属于正常叙事描写，不在禁止范围内。）
{% if GenreVisualHints %}
【类型视觉风格建议——优先级低于后文"画面风格强制规则"，两者冲突时以画面风格规则为准】
{{ GenreVisualHints }}
{% endif %}
{% if ImageStyleHint %}
【画面风格强制规则——所有 description 必须遵守，违反将导致整个输出被拒绝；此规则优先级高于上方类型视觉建议】
本项目画面风格设定为：**{{ ImageStyleHint }}**
① description 中的材质、光影、人物形象等一切视觉呈现方式须与此风格保持一致，但严禁在文本中写出风格名称本身或任何画质提升词——风格与画质词由系统在实际生成图片/视频时自动附加，description 只需专注于画面内容本身。
② 严禁在 description 中混用与本风格矛盾的描述（例如：风格含 anime/illustration 时禁止出现 photorealistic/photography/DSLR 等写实摄影词汇；风格含 realistic/photorealistic 时禁止出现 anime/illustration/cel shading 等动漫插画词汇）。
{% endif %}

【故事连贯性与镜头过渡——顶级强制规则，违反将导致整个输出被拒绝】
① **叙事链完整**：所有分镜必须构成一条前后贯通的故事链，每个镜头须与相邻镜头在场景、人物动作、情绪上保持逻辑衔接，严禁无铺垫的画面跳跃或情节断层。
② **场景切换必须有过渡**：若前后镜头发生地点变换（室内→室外、A地→B地），必须插入过渡镜头（角色起身/走出门/奔跑途中/环境空镜转场），禁止同一人物在相邻两镜中无缘由地出现在不同地点。
③ **transition 字段体现过渡逻辑**：动作/对话连贯接续 → cut / j-cut / l-cut；情绪积累或时间流逝 → dissolve / fade；节奏强转折 → push / wipe；禁止全片统一只写 cut。
{% if not ArcPlan %}④ **情绪弧线连贯**：全片情绪应呈现铺垫→积累→高潮→消化的演进节奏，通过 voice_lines 各行的 emotion/情感强度和 duration 的松紧变化体现；高潮前须有铺垫镜头，强情绪后须有缓冲镜头，不得从平静无过渡地直接切到极度紧张。
{% endif %}⑤ **人物状态前后一致**：同一角色在相邻镜头中的服装、伤势、持物、位置须保持连续性；若人物状态发生变化（如受伤、换装），须在前序镜头中有对应动作铺垫。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第三部分・镜头语言与视觉字段规范
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【字数上限速查】description 100-250字 | voice_lines[].content：旁白行≤45字，台词行≤35字 | sfx_tags.prompt 15-40字

{% if VideoMode == "slideshow" %}
【视频类型：图片解说——每个分镜只对应一张静态画面】
- 本片最终只生成静态图片（+ 轻微 Ken Burns 缩放/平移），不生成 AI 视频，因此每个分镜内只能呈现一个动作/一个画面瞬间，禁止在同一分镜的 description 中描述连续的多个动作或镜头内切换——若原文内容包含多个连续动作，须拆成多个分镜，每个分镜各自定格在其中一个瞬间。
- camera_type 以 static 为主即可（对应静态图 + 轻微推拉/平移的 Ken Burns 效果），不必满足后文"static 不超过 40%"的多样性要求；仅在画面确有明显纵深/横向张力时才选 push/pull/pan 等其他值。
{% else %}
【视频类型：视频动画——单个镜头可覆盖同一场景内的多个连续动作/运镜节拍】
本片会为每个分镜生成一段连续 AI 视频，因此不必为场景中的每个细分动作都拆出新镜头：只要能在该镜头的时长与一次运镜内合理呈现，一个分镜的 description 可以覆盖同一场景内多个连贯的动作或情绪推进节拍。以下 camera_type / transition 规则照常适用。
- 【对白/旁白融入画面描述——video 模式专属】若该镜头 voice_lines 非空，description 的 [角色区块] 除角色精确名称+画框位置+身体姿态与动作+面部表情外，还须引用该行的 content 文字内容，并紧跟其表演提示（语气特征 + 眼神朝向 + 配合动作），供 AI 视频模型据此生成匹配的开口说话动作与神情演进，而非仅给出静态姿态描述。此处引用的文字须与 voice_lines 中对应行的 content 完全一致，不得改写或使用不同措辞。
  格式：角色开口道/旁白道："引用文字"——语气xx，眼神xx，配合动作xx
  ✅ 对白示例：秦始皇端坐龙椅，居高临下俯视殿下扶苏，开口道："你要带那个废物去上郡？"——语气轻蔑随意，眼神懒散地扫过赢昭，手指无意识地敲击扶手
  ✅ 旁白示例（无角色开口，画外音同步）：扶苏跪地垂首的背影，旁白道："那一年，他终于明白了沉默的重量。"——肩背微微塌陷，双手交叠置于膝上，指节泛白
{% endif %}
▸ camera_type（运镜方式）——必须体现多样性，禁止全部使用 static
  可选值（选其中一个填入）：static | push | pull | pan | track | crane_up | crane_down | follow | arc | tilt | whip_pan | zoom
  含义：static=固定机位，push=推镜头(向前推进)，pull=拉镜头(向后拉远)，pan=摇镜头(水平旋转)，track=移镜头(横向平移)，crane_up=升镜头，crane_down=降镜头，follow=跟镜头(跟随主体)，arc=环绕镜头(围绕主体环绕)，tilt=俯仰镜头(垂直俯仰)，whip_pan=甩镜头(快速甩动)，zoom=变焦镜头(焦距变化)
  分布建议：{% if VideoMode == "slideshow" %}以上方【图片解说】分支的建议为准{% else %}static 不超过全部分镜的 40%，动作场景用 push/follow/arc，对话场景用 pan/tilt，环境展示用 pull/track/crane_up{% endif %}

▸ description（画面描述——唯一的画面生成依据，AI 出图和出视频均直接使用此字段，同时也是人工参考的叙事摘要）
  - 100-250字，按以下区块顺序撰写，区块间用换行符（\n）分隔，使每个区块独立成行：
    [构图/景别]（远景/中景/近景/特写 + 平视/俯视/仰视）\n[角色区块，若该镜头有角色出场则必填，结构规范见后文【角色信息】]
  - ⚠️ 只描述可见画面内容：气味/声音/温度/触感等非视觉感官描写（如"弥漫着面包香""空气中飘着柴火气息"）严禁写入 description——AI 出图/出视频无法渲染这些信息，纯属浪费字数；此类感官线索若需保留，应写入 voice_lines 中的旁白行或 sfx_tags（音效），不得混入 description
  - ⚠️ 严禁在 description 中出现画面风格标签（如"真人电视剧风格""动漫风格""大师级构图"等）或画质提升词（如 masterpiece, best quality, 8K, 超高清等）——这些由系统在实际生成图片/视频时自动附加，description 只需专注于纯粹的画面内容
  - 示例（JSON 字符串中须写作真实换行符 \n）："中景，平视角度\n角色区块：凌云站立于前景居中，面向石门，右手垂握长剑剑尖向下，头部微扬凝视门洞深处，神情肃穆坚毅"


{% if ArcPlan %}
【本章情感弧线骨架——分镜生成的叙事指南，必须遵循】
{{ ArcPlan }}
📍 当前段落位置：本段内容约覆盖章节 {{ SegStartPct }}%–{{ SegEndPct }}% 的叙事进度。请据此定位当前段处于弧线的哪个阶段，并严格遵循该阶段的情绪强度与旁白/台词指令。

核心要求：
- 按照各阶段的 intensity（情绪强度）分配旁白密度：强度低的阶段可以留白，高强度阶段让台词和环境声承担情绪
- 各阶段的 narration_guide / dialogue_guide 是对应镜头的旁白/台词写作指令，必须执行
- climax_pct 位置附近的镜头，旁白量减少或留空，台词和画面承担高潮情绪
- silence_shots 指定的位置，voice_lines 填 []（纯视觉/音效镜头）

{% endif %}
{% if BeatSheet %}
{% if StoryboardMode == "concise" %}
【本段情节节拍表——简洁模式：只覆盖关键剧情，重要度=minor 的节拍直接跳过】
以下是本段原文中每一个可视化叙事单元，已按重要度标注。**简洁模式下，只需为标注「重要度：major」的节拍生成对应分镜；标注「重要度：minor」的节拍（氛围铺垫、次要寒暄、重复性动作等）直接跳过，不生成分镜，也不必用旁白带过**——本模式的目标是产出更短、只保留主线剧情的版本：
{% for b in BeatSheet %}{{ b.No }}. [{{ b.BeatType }}] {{ b.ContentSummary }}{% if b.Location %} ｜地点：{{ b.Location }}{% endif %}{% if b.Characters %} ｜角色：{{ b.Characters }}{% endif %}{% if b.Importance %} ｜重要度：{{ b.Importance }}{% endif %}

{% endfor %}
⚠️ 生成前请先统计标注为 major 的节拍数量：若 major 节拍数不超过 {{ ExpectedShots }}，按一拍一镜正常生成；若 major 节拍数超过 {{ ExpectedShots }}，须将同一场景内连续发生、可视觉上共存于同一镜头的相邻 major 节拍合并为一镜（在该镜头 description 中依次呈现多个动作/信息点），直到总镜头数贴近 {{ ExpectedShots }}，禁止为了"完整覆盖"而放任分镜数量远超目标。合并/跳过之间仍需通过 transition 字段做合理的场景/情绪过渡衔接（跳过 minor 节拍不等于允许无铺垫跳切）。
{% else %}
【本段情节节拍表——逐拍覆盖地图，禁止跳过任何节拍】
以下是本段原文中每一个可视化叙事单元（动作/对话/场景切换/情绪转折），按顺序排列。**每一个节拍必须有对应的分镜覆盖，禁止合并相邻节拍或跳过任何节拍**：
{% for b in BeatSheet %}{{ b.No }}. [{{ b.BeatType }}] {{ b.ContentSummary }}{% if b.Location %} ｜地点：{{ b.Location }}{% endif %}{% if b.Characters %} ｜角色：{{ b.Characters }}{% endif %}{% if b.SuggestedShots %} ｜建议镜头数：{{ b.SuggestedShots }}{% endif %}

{% endfor %}
⚠️ 生成前请先对照节拍表通读一遍章节内容，确认每个节拍都在计划中有对应镜头，再开始逐拍生成分镜。
{% endif %}

{% endif %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第四部分・语言、旁白与台词
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 【语言强制规则——最高优先级，任何其他指令均不得覆盖此规则】
- voice_lines 中旁白行（character=""）：content 必须使用简体中文，严禁混入任何英文单词或其他外语，无论角色或场景设定如何。
- voice_lines 中台词行（character≠""）：content 语言须与角色在【角色信息】中标注的"台词语言"保持一致；未标注则默认简体中文。
- description 字段须使用简体中文撰写，不得混入英文（画质/风格提升词由系统在实际生成时自动附加，description 中不应出现）。

【旁白-画面对位原则】画面说环境/气氛，旁白说情感/时间/因果——严禁旁白重复画面已传达的内容：
  - ✅ 画面：古老庭院 → 旁白："三年前他也是这样进来的，只是那次，身边还有人。"（时间/情感维度）
  - ✅ 画面：夜深的房间 → 旁白："她数过三百六十个这样的夜晚，每一个都比上一个更难熬。"（人物内心）
  - ✅ 画面：空旷的战场 → 旁白："没有人知道那场仗到底是谁赢了。"（主题/因果）
  - ❌ 画面：破败庭院 → 旁白："废墟沉默，岁月蹉跎。"（同义重复，作废）
  - ❌ 画面：角色低头看着怀中的东西，嘴角带笑 → 旁白："她低头看着怀里的东西，脸上带着笑容。"（旁白只是把 description 里已写明的动作/表情复述一遍，没有提供画面之外的新信息——作废；正确做法是删掉这条旁白行，或改写为解释"为什么笑""这一刻在想什么"的心理/因果信息）
  ⚠️ 自检方法：写完 voice_lines 中每条旁白行的 content 后，逐字对照该镜头 description 中的动作/表情描述——若 content 里超过一半的实词（动词/名词/形容词）能在 description 中找到同义表达，判定为重复，必须删除该行或改写为画面之外的信息（时间流逝/心理活动/因果关系/伏笔），不得仅因文字不完全相同就视为合格。

{% if VideoMode == "slideshow" %}
【图片解说模式——旁白为主导】本模式以旁白行（character=""）为主要信息与情感载体。原文中非关键的次要对话（寒暄、背景交代、无独立戏剧功能的应答）应提炼进旁白行概括呈现，不必逐句拆分成独立台词行；台词行（character≠""）仅保留给原文中最关键、最具冲击力、能独立成为记忆点的台词。这样可以避免大量单句对白静态图连续堆叠导致的视觉单调。⚠️ 仍须遵守"原作优先"：旁白概括时须忠于原文语义和信息，不得编造原文没有的内容或改变对话的实际立场。
{% else %}
【视频动画模式——对白为主导】本模式以角色台词行（character≠""）与表演为主要叙事手段，旁白行（character=""）仅用于连接段落、时间过渡、无法通过表演直接呈现的心理活动或场景交代。若原文包含转述/间接引语（如"他让她别再追问"），且该处戏剧张力足够支撑一个独立镜头，可将其还原为角色直接开口的台词行，而非统一压缩进旁白行；⚠️ 仍须忠于原意，不得虚构原文没有的内容或改变角色的实际态度/立场。
{% endif %}

▸ voice_lines（旁白与台词的统一字段——每个元素为 {"character": "", "content": "", "emotion": ""}）
  ① 一个镜头的 voice_lines 最多两条：一条 character="" 的旁白行 + 一条 character=角色名 的台词行，二者可同时存在（旁白画外音与角色开口同时发生）；镜头完全无声时 voice_lines 填 []
  ② 每个镜头只允许一个角色说话——原文中两个角色连续对话时，必须拆分为两个独立分镜，每镜一条台词行
  ③ 拆分示例：凌云说"你敢！"→ 一个镜头（voice_lines=[{character:"凌云",content:"你敢！",emotion:"..."}]）；敌将回答"我就敢！"→ 下一个独立镜头
  ④ 单人连续台词不超过2句：若某角色需要传达多句信息/警示（如叮嘱、说明），须拆分为多轮对话并插入对方的反应或追问，不得让一个角色连续说完全部信息后另一角色才回应；如原文本身是单人大段独白且无法自然拆分互动，需在旁白行或 description 中补充对方的即时反应（点头、皱眉、插话等）来打破"信息宣读"感
     - ❌ 禁止：某角色连续说完多句叮嘱/说明后，对方才回应一句——应在中途插入对方的表情/动作反应镜头，或让部分内容以对方追问的形式带出

【原文优先总则——旁白行与台词行共同遵守，凌驾于两者下方各自的其他规则之上】旁白与台词都必须尽量取材于【章节内容】原文，禁止脱离原文自由发挥或自行创作；取材方式和字数限制按下方各自条目执行。

▸ voice_lines[] 中 character="" 的行（旁白内容）——有画外音旁白时填写一条；无旁白内容时不填该行
  - 观众"听到"的旁白内容，第三人称叙事视角，追求画面感
  - 旁白须优先提炼原文中的叙事语句、环境描写和人物心理活动，尽量保留原文的措辞和情感基调；禁止将 description 的画面描述改写成旁白
  - ⚠️ 字数限制：**不超过45字**，一句话或由逗号连接的一个完整意群，不得分句，不加省略号或破折号结尾
  - 严禁出现以下词汇：镜头、画面、特写、推进、切换、转场、画外音、摄影机、定格
  - 严禁以角色名称开头，如"妈妈说："、"凌云道："等格式——旁白是旁白，character 字段留空即表明是旁白，不得把角色名写进 content
  - **⚠️ 语法完整性（违反直接作废）**：每句必须是包含谓语动词的完整句，严禁名词/形容词短语堆砌：
    ❌ "三年的等待，一道伤疤。" / "月光，寒意，沉默。" / "风起，云涌。"（无谓语——作废）
    ✅ "三年的等待在这一刻化作无言的伤疤。" / "月光将寒意从石板一寸寸压入她的脚底。"

▸ voice_lines[] 中 character≠"" 的行（角色台词）——有角色开口说话时填写一条
  - character 填该角色的精确名称（须与【角色信息】列表一致），content 只填台词内容本身，不得写成"角色名：内容"的拼接格式（角色名已由 character 字段独立承载）
  - ⚠️ 台词语言：content 须与该角色在【角色信息】中标注的"台词语言"保持一致。简体中文角色写中文台词，English 角色写英文台词，日文角色写日文台词，以此类推。
  - 台词必须尽量使用【章节内容】中的原文对白，禁止无故改写、替换或自行创作台词：
    ① 若原文对白字数 ≤ 35字：**原文照搬，一字不改**
    ② 若原文对白字数 > 35字：保留最核心的情绪/信息点，在35字以内**精简**，但必须保留原文的关键词和语气，不得改变台词的立场或情绪方向
    ③ 若该镜头无对应原文直接引语对白（纯动作/场景镜头）：改用旁白行，不要自创台词{% if VideoMode != "slideshow" %}；但若原文以转述/间接引语描述了该处对话内容，参见上方【视频动画模式——对白为主导】的还原规则{% endif %}
    ❌ 原文："你以为走了这件事就当没发生过？三年，整整三年，你欠我一个交代！" → 改成"你欠我一个交代"（丢失情绪层次——作废）
    ✅ 原文同上 → 精简为："你以为走了就没事了？三年，你欠我一个交代。"（保留质问语气和时间信息）
  - ⚠️ **语法完整性——台词必须是语义完整的口语句子**：
    台词内容必须构成完整的口语表达，让听者能独立理解其意思，不依赖上下文猜测。
    ❌ "你……"（省略号结尾，语义残缺——作废）
    ❌ "算了。"（信息量为零，无戏剧功能——作废）
    ❌ "不可能的。"（断言式，无具体内容，无张力——作废）
    ✅ "你以为走了这件事就当没发生过？"（完整质问，有具体指向）
    ✅ "算了，这条路你从来就没打算带上我。"（完整陈述，有情绪和信息）
  - 避免完全无意义的泛用台词（任何场景任何角色都能说的话）：
    ❌ "我知道了" / "你说得对" / "好的" / "没关系" / "走吧" / "我明白了"

▸ voice_lines[].emotion（该行的配音情感基调——每条 voice_lines 都必须填写，不得为空）
  - 根据这条旁白/台词的情绪内容，分析并填写最贴切的情感标签，驱动 TTS 配音情感
  - 可选值（选其中一个）：平静 | 悲伤 | 紧张 | 愤怒 | 喜悦 | 浪漫 | 惊恐 | 压抑 | 释怀 | 悬疑 | 振奋 | 恐惧 | 得意 | 讽刺
  - 判断依据：旁白行按其情绪渲染方向判断（如描写分离→悲伤，描写危机→紧张，描写重逢→喜悦）；台词行按角色说话时的情绪基调判断（如质问/控诉→紧张或愤怒，道别→悲伤或压抑，告白→浪漫）
  - ⚠️ 不得全部填"平静"，须结合上下文认真分析每一行的情绪

▸ original_text（该镜头对应的章节原文片段——必填，用于人工核对分镜与原著的对应关系）
  - 从【章节内容】中逐字摘录该镜头所依据的原文片段（可以是连续的一句或几句），不得改写、概括或替换措辞
  - 若该镜头是本工具补充的过渡/转场镜头（如时间跳跃镜头），原文中无直接对应文字，则填该镜头衔接的前后原文片段中最贴近的一句
  - 该字段不受字数上限约束，但应控制在覆盖该镜头内容的最小必要范围
  {% if StoryboardMode == "faithful" %}- **忠于原文模式下，此字段不是事后摘要，而是生成起点**：先摘录 original_text，再据其生成 voice_lines/description，参见上方【忠于原文模式】强制规则⑥{% else %}- 该字段仅供人工溯源核对，不用于画面/音频生成{% endif %}

【配音与节奏——自动判定，二者可同时出现于同一镜头】
- 角色开口说话用 character≠"" 的台词行，场景叙事/过渡/心理描写用 character="" 的旁白行；时间跳跃镜头的旁白行只写时间词（≤10字，见后文四级分类）；纯视觉/音效镜头（voice_lines=[]，全片占比≤15%）不填任何行；其余镜头 voice_lines 不得为空数组
- duration 由内容强度自主决定：动作/打斗 1.5–3s，对话 3–5s，情绪铺垫 5–8s，环境全景 6–10s；全片节奏应随情节起伏动态变化，不设总时长上限，不得为压缩时长而裁减情节

⚠️ 强制要求：
{% if StoryboardMode == "concise" %}
① **分镜数量严格贴近目标，禁止因 major 节拍多而放任超量**：{{ ExpectedShots }} 是压缩后的目标镜头数，上浮不得超过15%；以完整覆盖 BeatSheet 中标注 major 的节拍为基础标准，但若 major 节拍数量超过目标镜头数，必须合并同场景/同时段内相邻的 major 节拍到同一镜头中表现，而非逐拍各开一镜；minor 节拍允许跳过（不需要合并进旁白或另起镜头），这不算"压缩情节"，是简洁模式的预期行为
{% else %}
① **分镜数量服从内容，不设上限**：{{ ExpectedShots }} 仅为参考下限，以完整还原章节情节为唯一标准，不得为控制数量而合并、跳过或压缩原文情节
{% endif %}
② **不得添加原文没有的情节**：禁止基于"合理推断"自行补充原文未描写的场景或对话

{% if IsFirstChapter and IsFirstSegment %}
【⚠️ 全书开场——第一章第一镜专属强制规则，违反将导致整个输出被拒绝】
这是《{{ NovelTitle }}》全书的第一个分镜。观众对这个世界一无所知，必须用前几个镜头完成"落地"，才能让后续情节有依托。

① **第1镜必须是建立大景**：description 须以远景/全景构图开场，展现地点全貌，严禁以中景/近景/特写构图开场。
② **第1镜严禁直接开场对白**：voice_lines 中不得出现 character≠"" 的台词行，角色未出现在观众视野前不能开口说话。
③ **第1镜 description 必须包含三要素**：地点全貌 + 时间/天色（晨/昏/夜/日正当中）+ 情绪底色（荒芜/繁华/危机/宁静等），让观众在3秒内完成视觉落地。
④ **前3镜遵循构图漏斗**：远景/全景（建立环境）→ 中景（人物入场）→ 近景/特写（情绪落点）。不得在第1镜就给特写。
{% if WorldviewDesc %}

【世界观参考——用于第1镜建立镜头的视觉基调设计】
{{ WorldviewDesc }}
{% endif %}
{% endif %}
{% if WorldState %}
【跨段世界状态——本段承接上一段结束时的场景状态，必须维持一致直至原文明确说明变化】
- 当前位置：{{ WorldState.Location }}

⚠️ 强制要求：本段开篇须延续以上地点，不得无故切换场景；若原文有明确的地点/时间切换，必须按四级时间转场规则插入过渡镜头。

{% endif %}
{% if PrevShots %}
【⚠️ 上一段末尾分镜——本段必须无缝衔接，违反将导致输出被拒绝】
以下是上一段最后若干个分镜的完整状态。**本段第 1 镜必须在地点、情绪氛围、人物动作状态上与最后一个分镜自然延续**，严禁任何叙事跳跃或内容重复。
{% for ps in PrevShots %}
  第{{ ps.ShotNo }}镜 [运镜:{{ ps.CameraType }}{% if ps.Location %} 地点:{{ ps.Location }}{% endif %}]
  画面/旁白：{{ ps.NarrOrDesc }}{% if ps.Dialogue %}
  台词：{{ ps.Dialogue }}{% endif %}
{% endfor %}
🚨 衔接强制要求：
- 本段第 1 镜的 transition 须为 cut（无缝直接接续）或 dissolve（情绪溶解过渡），不得是 fade（黑场淡入）。
- 本段第 1 镜必须延续上方最后一个分镜的地点和情绪氛围，不得自行发起新的场景或情绪跳变。
- 若上一段末尾人物正在进行动作或对话，本段首镜须接续该动作/对话的完整语境，不得跳过。
{% endif %}
{% if IsFirstSegment and not PrevShots %}
【⚠️ 第1镜内容锚定——章节开篇精确对齐规则，违反将导致输出被拒绝】
**第1镜必须忠实还原下方章节内容（Content 字段）前约100字所描述的场景、人物状态或动作。**
- 阅读 Content 的开头约100字，将其作为第1镜的叙事素材。
- 第1镜的旁白行/description 须直接取材于这段开篇文字，覆盖其中至少一个核心意象或动作。
- 严禁跳过开头情节、提前展示后续内容，或用与原文无关的环境空镜替代。
- 若开篇文字描述的是对话或动作，第1镜须呈现该状态的视觉化，而非绕开它另起炉灶。
{% endif %}
{% if Characters %}
【角色信息】
⚠️ 角色名称强制规则（与核心红线①一致，全字段适用）：
`description` 凡涉及角色处，必须使用以下列表的精确名称。
⚠️ 【角色在画面描述中的强制要求——违反将导致图像/视频生成失败】
- 若该镜头有角色出场，`description` 中必须包含完整的【角色区块】：角色精确名称 + 画框位置（前景/中景/背景+方位）+ 身体姿态与动作 + 面部表情 + 与环境的空间关系
{% for c in Characters %}- {{ c.Name }}（{{ c.Role }}）{% if c.Description %}：{{ c.Description }}{% endif %}

  [台词语言]: {{ c.DialogueLang }}{% if c.InnerConflict %}
  [内在矛盾]: {{ c.InnerConflict }}（台词潜台词的根源，角色说话时内心的撕裂与压制）{% endif %}{% if c.CoreDesire %}
  [核心渴望]: {{ c.CoreDesire }}（驱动该角色一切行动与台词的最深动机）{% endif %}{% if c.VisualPrompt %}
  [外貌关键词参考——写入 description 角色区块时使用]: {{ c.VisualPrompt }}{% endif %}
{% endfor %}
⚠️ 台词生成强制规则：每句台词必须与该角色的内在矛盾/核心渴望相一致，体现角色独特的说话方式和潜台词——禁止生成任何角色都能说的通用台词。
⚠️ 【角色重复出场的表演差异化规则】：同一角色（尤其反派/多次登场的角色）在不同镜头中的表情/眼神/语气等表演描写，禁止在全片中反复套用同一组固定形容词组合（如反复写"狡诈的微笑""琥珀色的狼眼""表面关切实则暗藏贪婪"）。每次登场应根据该场戏在心理博弈/情节推进中所处的阶段，体现表演上的层次递进（试探期→得逞期→紧张期→暴露期等），用不同的具体细节表现，而非重复同一套标签化描述。
{% endif %}
{% if Anchors %}
【已命名场景】
⚠️ 规则：`location` 字段必须从以下名称中精确选择，不得自造新名称。
{% for a in Anchors %}- {{ a.Name }}: {{ a.Description }}
{% endfor %}
{% endif %}
{% if Items %}
【本章道具/道具】
⚠️ 规则：当某道具在画面中出现时，`description` 中须使用以下精确名称，不得自造新名称。
{% for item in Items %}- {{ item.Name }}{% if item.Owner %}（持有者：{{ item.Owner }}）{% endif %}{% if item.Location %}（位置：{{ item.Location }}）{% endif %}
{% endfor %}
{% endif %}
{% if PlotPoints %}
【本章剧情线（旁白须体现叙事张力）】
{% for pp in PlotPoints %}- [{{ pp.Type }}] {{ pp.Description }}
{% endfor %}
{% endif %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第五部分・时间跳跃转场镜头（四级分类强制规则）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
章节内容中出现时间间隔标志词时，根据跨度分级处理，不得统一套用同一模板。

| 级别 | 识别词示例 | 处理方式 | voice_lines（仅一条旁白行，无台词行） | camera_type | duration | transition |
|---|---|---|---|---|---|---|
| 一级·极短跨度（分钟级） | 片刻后、须臾、稍后、半晌、少顷、一炷香后、一盏茶工夫、转眼间 | ❌ 不插入专用镜头，在紧接的下一个正常分镜中带入 | 自然带入时间词即可（如"片刻后，他回到了……"） | 不适用 | 不适用 | 该镜 transition 写 j-cut |
| 二级·短跨度（小时~次日级） | 翌日、次日、天亮后、黎明时分、黄昏时、入夜后、当夜、是夜、子夜、一夜之后 | ✅ 插入1个转场镜头 | 时间词本身，结尾加句号（如"翌日。"），不超过10字 | static 或 pull | 2.0~3.0秒 | dissolve |
| 三级·中跨度（数日~数月级） | 三日后、数日后、五日后、七日后、半月后、十日后、一月后、月余、数月后、三月后、半年后 | ✅ 插入1个转场镜头，画面+声音双重时间信号 | 时间词本身，结尾加句号（如"三日后。"），不超过10字 | static 或 pull | 3.0~4.0秒 | dissolve |
| 四级·长跨度（一年及以上） | 一年后、两年后、三年后、数年后、多年后、五年后、十年后、数十年后、百年后 | ✅ 插入2镜序列（shot_no连续编号，不跳号）：镜头A环境大景 + 镜头B时间痕迹特写 | A镜：时间词本身，结尾加句号（如"三年后。"），不超过10字；B镜：""（纯视觉，让画面说话） | A镜：pull 或 crane_up（升镜强化岁月感）；B镜：static 或 push（缓缓推近） | A/B镜均 3.0~5.0秒 | A/B镜均 dissolve（B镜溶解进下一段正常叙事） |

以上二~四级 characters：镜头中不出现角色（description 中不写角色区块）。

description 补充要求（二~四级通用）：大远景+俯瞰或高角度构图，无角色，选择体现时间流逝的意象，情感基调决定具体选择：
  ▸ 悲剧/压抑基调 → 花朵凋谢/树叶枯落/青苔爬墙/积尘加厚（二级另有光线变化画面：日出橙红晨光/黄昏金红暮色/月升银白夜空；四级另有荒草蔓延、屋顶坍塌、庭院空置）
  ▸ 励志/成长基调 → 新芽萌发/枝条繁茂/路面被踩出深痕（四级另有同一地点焕然一新、植被茂盛、人迹增多）
  ▸ 中性/思念基调 → 季节交替（绿→黄→白→绿）/月亮盈亏/灯火从明到灭（四级另有物是人非，场景保存完好但空无一人）

四级镜头B（特写）额外要求：近景/特写，平视或低角度（仰视渺小物体，强化时间压迫感），选择一件承载时间重量的具体物体特写：青苔从无到厚厚一层覆盖石阶 / 同一把椅子木材从新到开裂发灰 / 同一棵树从细幼苗到粗壮树干（树皮刻字已被生长撑开变形）/ 同一把剑刃面从光亮到锈迹斑斑（若剑象征主角状态）。

sfx_tags（二~四级通用，完整 JSON 数组格式，选1~2个）：二级用 birdsong（日出）/ crickets_night（入夜）/ wind_night（子夜）/ wind_gentle（清风），例如 [{"tag":"birdsong","type":"ambient","prompt":"清晨第一缕鸟鸣，空旷山林中远近交错的啁啾声，清新宁静"}]；三/四级用 bell_ring / wind_gentle / silence_deep / echo_distant，例如 [{"tag":"wind_gentle","type":"ambient","prompt":"岁月悠悠，风声轻拂旧地，带着无声流逝的沧桑感"}]。

此类转场镜头的 shot_no 连续编号（不跳号）。第四级（长跨度）的2镜序列可使总分镜数超过 {{ ExpectedShots }}，最多额外增加10镜（含前三级的单镜转场）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第六部分・输出前自检与交付格式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【输出前连贯性自检——必须执行，通过后再返回 JSON】
在生成完所有分镜草稿之后、返回 JSON 之前，逐条按对应规则回查，未通过须先修正再输出：
1. 按【故事连贯性与镜头过渡】①②：相邻分镜是否有逻辑连接？同一人物是否无缘由换地点？
2. 按【故事连贯性与镜头过渡】③④：transition 是否多样化（不得全篇 cut）？全片情绪是否形成弧线？
3. 若本段含【上一段末尾分镜】：首镜是否满足其后的🚨衔接强制要求？
4. 按【时间跳跃转场镜头】四级分类：本段出现的每个时间标志词是否都用对应级别的字段组合处理？
5. 按【分镜分布原则】：镜头数量是否按戏剧强度分配（无一段超过50%）？低强度段落是否已压缩，有无对同一静止状态/同一动作反复开出3个以上雷同镜头？
6. 按 voice_lines 的原文优先原则、original_text 的逐字摘录要求：是否都满足？{% if StoryboardMode == "faithful" %}忠于原文模式下，还需确认 voice_lines 各行确由该 original_text 转录而来，而非独立构思后回填一段相似原文。{% endif %}
7. 按【旁白-画面对位原则】自检方法：逐条检查 voice_lines 中每条旁白行的 content 是否与所在镜头 description 的动作/表情描述同义重复？有则删除该行或改写。
8. 按【角色重复出场的表演差异化规则】：同一角色多次出场时，表情/语气描写是否出现了重复的固定形容词组合？有则改写为体现心理阶段递进的具体细节。
{% if IsFirstChapter and IsFirstSegment %}
9. 按【全书开场】①-④：第1镜是否全部满足（远景开场/无开场对白/三要素/构图漏斗）？
{% endif %}

请将章节内容分解为**约{{ ExpectedShots }}个**分镜（每个重大动作、对话轮次、场景切换须单独成镜）。
⚠️ 注意：以内容完整覆盖为首要目标，实际分镜数应达到约 {{ ExpectedShots }} 个；时间转场镜头（按四级规则插入）可使总数在此基础上超出，以叙事完整性为准。只返回JSON数组，不要任何额外说明或markdown代码块。

【以下字段（sfx_tags）是 JSON 输出的结构化数据字段，与上方的 description 等文本字段同等重要，必须按规范填写】

【sfx_tags 填写规则】
sfx_tags 是该镜头的结构化音效标签列表，每个元素含三个字段：
- tag：简洁的英文搜索词（如 wind_gentle、sword_clash，用于音效库匹配）
- type：音效类型，固定取 ambient（环境底层音，循环）/ action（动作触发音，单次）/ emotion（情绪点缀音）
- prompt：中文自然语言描述（15-40字），供 AI 文生音效直接使用。描述时包含以下要素：
  ① 声音来源（什么在发声，如“石板缝隙间的细小水流”而非“水声”）
  ② 时间特征：持续循环型 / 单次触发型 / 渐入渐弱型
  ③ 空间特征：近距离贴耳 / 中景距离 / 远处传来 / 空旷回响 / 密闭空间共鸣
  ④ 音色质感：低沉浑厚 / 高亢清脆 / 沙哑粗粝 / 清澈空灵 / 厚重沉稳
  ✅ 示例：“古旧石廊深处单次触发的沉重厚木门关闭声，带明显空间回响，在石壁间余韵持续约1秒”
  ✅ 示例："持续铺底的低频森林环境音，充满厚重沉稳感，无明显单点声源，密度均匀"
  ❌ 示例："带历史感的木门声"（无时间/空间/音色信息）
每镜头选 1-4 个最贴合的标签，不得照抄示例。纯对话/静态镜头留空数组 []。

⚠️⚠️⚠️ 格式示例警告：以下 JSON 示例中所有人名（如"凌云"、"白雪"）、对白（"三年了……"）、场景（石庭废墟、门洞、玉坠）均为【虚构占位内容】，与本次章节完全无关。你的输出中绝对不得出现这些名字或照搬这些场景——只参考字段名称和数据类型结构。你必须完全根据下方【章节内容】生成分镜。

格式示例（以下仅展示2个分镜作为格式参考，实际须生成{{ ExpectedShots }}个）：
[
  {
    "shot_no": 1,
    "description": "中景，平视角度，破碎灰色石板庭院，左侧倒卧石质香炉，铜绿风化剥落，居中约1.8米花岗岩石碑，碑面有对角裂缝；两根石柱夹持约3米宽石门洞，左柱完好有莲花柱头浮雕，右柱齐断，横梁坠落碎石堆",
    "voice_lines": [{"character":"","content":"苔痕浸透了每一块石板，废墟用沉默记录着无人问津的岁月。","emotion":"压抑"}],
    "original_text": "庭院早已荒废多年，石板缝隙里长满了青苔，那尊香炉倒在墙角，铜绿爬满了炉身。",
    "camera_type": "pan",
    "duration": 5.0,
    "location": "<已命名场景中的精确名称，或自然语言描述>",
    "transition": "cut",
    "sfx_tags": [{"tag":"forest_ambient","type":"ambient","prompt":"古旧石庭院四周松林中低沉连绵的森林环境音，透出一种荒废已久的寂静"},{"tag":"wind_gentle","type":"ambient","prompt":"穿过石门洞的一缕轻柔穿堂风，带有幽凉气息"},{"tag":"stone_door","type":"action","prompt":"沉重石门在废墟中发出低沉摩擦声，带有厚重的历史感"}]
  },
  {
    "shot_no": 2,
    "description": "中景，平视角度\n角色区块：凌云站立于前景居中，面向石门洞，右手垂握长剑剑尖向下，头部微扬凝视门洞深处，开口道：\"三年了，我终于回来了。\"——语气低沉喑哑，眼神死死锁住门洞深处，说话时下颌微微收紧",
    "voice_lines": [{"character":"凌云","content":"三年了，我终于回来了。","emotion":"释怀"}],
    "original_text": "凌云站在石门前，右手垂握长剑，剑尖朝下，抬头望着门洞深处，忽然低声道：\"三年了……终于回来了。\"",
    "camera_type": "static",
    "duration": 4.0,
    "location": "<已命名场景中的精确名称，或自然语言描述>",
    "transition": "dissolve",
    "sfx_tags": [{"tag":"wind_gentle","type":"ambient","prompt":"从石门洞深处吹出的穿堂风，带有石廊独特的低沉共鸣"}]
  }
  ... （继续输出剩余 {{ ExpectedShotsMinus2 }} 个分镜，格式与上述完全相同）
]
{% if Content %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【章节内容】——这是本次必须转化为分镜的唯一故事素材，上方示例中的人物和场景均与此无关，请完全忽略示例中的具体内容
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{ Content }}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
请将上述【章节内容】分解为 {{ ExpectedShots }} 个分镜（只返回 JSON 数组，不要任何额外说明）。
{% endif %}
