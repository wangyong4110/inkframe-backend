请从以下章节内容中提取本章出现的道具/道具，生成道具档案。

# 小说信息
- 标题：《{{ NovelTitle }}》
- 类型：{{ Genre }}

# 已有道具（不要重复提取）
{% for name in ExistingNames %}- {{ name }}
{% endfor %}

# 本章内容
{{ Content }}

# 要求
1. 提取本章中**被明确命名且在故事中有一定重要性**的道具（武器、宝物、道具、文件、器物等）；不要求多次出现，一次出现但对情节有影响即可提取
2. **明确排除**：普通家居用品（门、锁、碗、桌椅、门栓等）、无名路人道具、纯环境摆设、完全不影响情节的一次性道具
3. 不要重复已有道具列表中的道具
4. 不超过8个；若本章确实无符合条件的道具，输出空数组 []
5. location：道具当前所在位置（如"主角腰间"）；owner：当前持有者（如角色名），无法判断则留空字符串
6. visual_prompt：详细的中文图像生成提示词，严格控制在 80-150 个字符以内（部分图像模型会直接拒绝过长的提示词），只覆盖以下 4 项核心内容，不要涉及其他内容：
{% include "fragments/item_visual_dimensions.j2" %}
   不要包含陈设背景、风格/画风、质量/渲染标签——这些由生成流程另行注入
7. 严格输出 JSON 数组，不含任何说明文字

{% include "fragments/user_prompt.j2" %}
# 输出格式（JSON 数组）
[
  {
    "name": "道具名",
    "location": "主角腰间",
    "owner": "主角名",
    "visual_prompt": "上古修真长剑，玄铁锻造并融入星辉银矿，剑身哑光深黑。墨黑剑刃两面浮现淡蓝古老符文微光。直身双刃，长约三尺，剑尖略收，密布龙纹阴刻，青铜护手作展翼龙形，双目各嵌一颗赤色宝石，深炭色丝缠剑柄。剑刃周身缭绕淡淡寒霜雾气。"
  }
]
