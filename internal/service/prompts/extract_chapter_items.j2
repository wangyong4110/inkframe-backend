请从以下章节内容中提取本章出现的物品/道具，生成简要的物品档案。

# 小说信息
- 标题：《{{ NovelTitle }}》
- 类型：{{ Genre }}

# 已有物品（不要重复提取）
{% for name in ExistingNames %}- {{ name }}
{% endfor %}

# 本章内容
{{ Content }}

{% if UserPrompt %}
# ⚠️ 用户指令（最高优先级）

以下指令由用户直接下达，**凌驾于本提示词的所有默认规则之上**，必须无条件执行，不得以任何理由覆盖或忽略：

{{ UserPrompt }}

{% endif %}
# 要求
1. 只提取本章中**多次被提及或对情节有明显推动作用**的命名物品（武器、宝物、道具、文件、器物等）
2. **明确排除**：普通家居用品（门、锁、碗、桌椅、门栓等）、环境摆设、仅一次出场的路人道具、只在一句话中一次提及的物品
3. 不要重复已有物品列表中的物品
4. 不超过5个；若本章无符合条件的物品，输出空数组 []
5. category 只能是：weapon / treasure / tool / document / artifact / other
6. description：物品的详细说明，包含其在故事中的重要性、作用和特殊能力（1-2句）
7. visual_prompt 用简短英文描述外观，不超过60词
8. 严格输出 JSON 数组，不含任何说明文字

# 输出格式（JSON 数组）
[
  {
    "name": "物品名",
    "category": "weapon",
    "appearance": "一柄古朴的长剑，剑身刻有符文",
    "description": "主角的传家宝剑，能斩断任何束缚，是本章突破境界的关键道具",
    "location": "主角腰间",
    "owner": "主角名",
    "visual_prompt": "ancient longsword with carved runes on blade, glowing faint blue, ornate hilt"
  }
]
