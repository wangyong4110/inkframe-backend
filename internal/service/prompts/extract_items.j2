请从以下小说内容中提取所有重要道具/道具，生成详细的道具档案。

# 小说信息
- 标题：《{{ NovelTitle }}》
- 类型：{{ Genre }}

# 内容概要
{{ Summaries }}

# 要求
1. 只提取在故事中**多次出现**、对情节有重要推动作用的命名道具（如主角的武器、关键宝物、反复使用的道具、重要文书、核心法宝等）；最多 12 个
2. **明确排除**：普通家居用品（门、锁、碗、桌椅等）、环境摆设、一次性出场的场景道具、只在一句话中一次提及的道具
3. visual_prompt：详细的中文图像生成提示词，用于 AI 绘图。只覆盖以下 4 项核心内容，不要涉及其他内容，严格控制在 80-150 个字符以内（部分图像模型会直接拒绝过长的提示词，如需精简优先省略最不影响识别的细节）：
{% include "fragments/item_visual_dimensions.j2" %}
4. 不要包含陈设背景、风格/画风、质量/渲染标签——这些由生成流程另行注入
5. 若信息不足，结合小说类型合理推断，不要留空
6. 严格输出 JSON 数组，不含任何说明文字

# 输出格式（JSON 数组）
[
  {
    "name": "道具名称",
    "visual_prompt": "上古修真长剑，玄铁锻造并融入星辉银矿，剑身哑光深黑。墨黑剑刃两面浮现淡蓝古老符文微光。直身双刃，长约三尺，剑尖略收，密布龙纹阴刻，青铜护手作展翼龙形，双目各嵌一颗赤色宝石，深炭色丝缠剑柄，黑漆剑鞘。剑刃周身缭绕淡淡寒霜雾气。"
  }
]
