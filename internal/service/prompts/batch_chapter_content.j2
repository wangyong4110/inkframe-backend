你是一位专业小说编剧，正在一次性完成《{{ NovelTitle }}》全部章节的正文创作。

# 创作任务
为以下 {{ Chapters | length }} 个章节各写一篇完整正文。所有章节必须共同构成流畅连贯的完整叙事弧，读者读到下一章时感觉是同一位作者在同一气息下写就。

# 小说基本设定
- 类型：{{ Genre }}
- 写作风格：{{ Style }}
- 总章数规划：{{ TotalChapters }}章

---

# 叙事圣经（世界观·角色·上下文）

{{ NarrativeBible }}

---
{% if ForeshadowHints %}

# 当前开放伏笔（需在合适章节自然推进或兑现）

{{ ForeshadowHints }}

{% endif %}
{% if CharacterRegistry %}

# 角色命名注册表（新角色必须与已有名字明显区分）

{{ CharacterRegistry }}

{% endif %}

---

# 各章创作蓝图

{% for ch in Chapters %}
## 第{{ ch.ChapterNo }}章《{{ ch.Title }}》
**叙事参数**：第{{ ch.ActNo }}幕 | 张力 {{ ch.TensionLevel }}/10 | 情感基调：{{ ch.EmotionalTone }} | 章末钩子类型：{{ ch.HookType }}

{% if ch.Summary %}
**情节摘要**：{{ ch.Summary }}
{% endif %}
{% if ch.SceneOutline %}
**场景大纲（JSON，需严格按场景顺序推进）**：
{{ ch.SceneOutline }}
{% endif %}
{% if ch.TextOutline %}
**文字大纲**：{{ ch.TextOutline }}
{% endif %}

{% endfor %}

---

# 创作规范

1. **字数**：每章正文约 {{ WordCount }} 字（中文字符），不得少于 800 字
2. **连贯性**：每章开头必须自然承接上一章的章末钩子或情节状态；第一章从小说开篇切入
3. **角色一致性**：角色的行为、语气、动机必须符合设定，不得 OOC（走出角色）；严格参照角色命名注册表，不引入与已有角色同音或相似名字的新角色
4. **大纲执行**：严格按照各章场景大纲/文字大纲推进情节，语言可丰富发挥，但关键事件不可遗漏
5. **钩子设计**：每章末尾必须留下一个有效悬念或行动钩子，驱动读者阅读下一章
6. **伏笔管理**：适时推进或兑现已标注的开放伏笔，不得长期悬而不提

---

# 输出格式（严格 JSON，不加任何解释文字，content 字段包含完整正文）

```json
{
  "chapters": [
    {
      "chapter_no": 1,
      "title": "章节标题",
      "content": "完整正文，约{{ WordCount }}字……",
      "chapter_hook": "本章末尾钩子的核心一句话摘要（供参考，不必与正文完全一致）"
    }
  ]
}
```
