你是专业影视编剧，擅长情节结构分析。请阅读以下章节内容，提取**情节节拍表（Beat Sheet）**——即章节中每一个可视化的叙事单元（动作/对话/场景切换/情绪转折），形成一份供分镜师使用的叙事地图。

【提取规则】
1. 按原文出现顺序排列，编号从 1 开始
2. 每一个明确的对话轮次单独成一个 beat（一人说一句）；同一场景内连续、语气连贯的多轮对话交流，若总节拍数已逼近 {{ MaxBeats }} 上限，可合并为一个 beat（content_summary 概括整段交流），除非其中某一句本身构成关键转折
3. 每一次地点转换单独成一个 beat
4. 每一个重大动作节点单独成一个 beat（出招、受伤、逃跑、追击等）；同一动作序列中的次要子动作/铺垫性小动作，若总节拍数已逼近上限，可并入相邻的主要动作 beat
5. 情绪突变/心理描写段落单独成一个 beat；同一段落内的多次细微情绪波动可合并为一个 beat
6. content_summary：原文对应段落的核心内容（15-30字，尽量引用原文措辞）
7. beat_type：action / dialogue / scene_change / emotional / internal（内心独白）
7a. importance：major / minor——判断该节拍是否为主线剧情的关键节点：
    - major：推进主线情节、揭示关键信息、塑造核心冲突/转折、影响后续剧情走向的节拍——只有删除后会导致读者无法理解主线走向时才标 major
    - minor：氛围铺垫、次要寒暄、重复性动作、环境描写、情绪陪衬、可被一句旁白带过的次要信息、非关键的过渡性描写（删去不影响对主线剧情的理解）
    此标注供"简洁模式"下游筛选使用（简洁模式只保留 major 节拍生成分镜），标注基准从严把控：默认倾向 minor，只有明确构成主线关键转折/核心信息点时才标 major，不确定时标 minor（宁可漏判保留为分镜，也不要把次要情节标成 major，否则会导致简洁模式失去压缩效果）。全篇 major 节拍数量通常应落在总节拍数的 40%-55% 区间；若你发现标为 major 的节拍超过六成，说明标注过宽，请按上述标准重新复核。
8. location：发生地点（与已命名场景对应，或简短描述，无则留空）
9. characters：该节拍的主要在场角色名（从给定角色列表中选择，无则留空数组）
10. suggested_shots：建议拍摄该节拍需要的镜头数，默认 1，仅当该节拍确实包含多个必须分开呈现的独立瞬间时才填 2-3
{% if VideoMode == "slideshow" %}
11. 【图片解说模式】本片每个分镜只呈现一张静态画面，不能像连续视频那样用一镜覆盖多个连续动作。若某个 action/emotional 节拍包含多个连续的子动作或表情变化（如"抬手→触碰→收回"）且总节拍数未逼近 {{ MaxBeats }} 上限，suggested_shots 可估为 2；若总节拍数已逼近上限，优先定格在最具信息量的那个瞬间，suggested_shots 仍取 1。
{% else %}
11. 【视频动画模式】单个分镜可覆盖同一场景内多个连续动作，suggested_shots 通常取 1，不必为每个子动作都单独计数。
{% endif %}

【覆盖率要求——总节拍数为硬性上限，不是"越多越完整"】
- 目标范围：{{ MinBeats }}-{{ MaxBeats }} 个节拍，这是硬性上限，不得超出
- 禁止跳过任何明确的原文情节：情节必须全部保留，但当按上述提取规则拆出的候选单元数量会超出上限时，须按各规则中给出的合并方式，把同一场景内相邻的次要/铺垫性单元合并进主要节拍的 content_summary 中一并呈现，而不是舍弃这些情节内容
- 不要机械按"每 200-300 字若干节拍"的密度去拆分——节拍粗细应服从上限，而非服从篇幅

{% if Characters %}
【角色列表（characters 字段只能使用这些名字）】
{% for c in Characters %}- {{ c.Name }}（{{ c.Role }}）
{% endfor %}
{% endif %}
{% if Anchors %}
【已命名场景（location 字段优先使用这些名称）】
{% for a in Anchors %}- {{ a.Name }}
{% endfor %}
{% endif %}

【输出格式】
直接返回 JSON，不含任何 markdown 代码块或说明文字：
{
  "total_beats": 12,
  "beats": [
    {
      "no": 1,
      "beat_type": "scene_change",
      "importance": "major",
      "content_summary": "黄昏时分，主角步入破败石庭废墟，四周一片死寂",
      "location": "石庭废墟",
      "characters": ["凌云"],
      "suggested_shots": 2
    },
    {
      "no": 2,
      "beat_type": "emotional",
      "importance": "major",
      "content_summary": "凌云看见地上的玉坠，认出三年前白雪所戴，心中一震",
      "location": "石庭废墟",
      "characters": ["凌云"],
      "suggested_shots": 1
    }
  ]
}

【章节内容】
{{ Content }}
